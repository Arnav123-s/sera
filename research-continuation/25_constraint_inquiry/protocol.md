# CI-001: role-bound continuous imagination on the actual owner

Frozen before the first new fit or final evaluation. Parent: SERA 29f50f9,
inquiry revision 000011-5352229ee6c5. Kavi remains 50f743c. The original v5
language and numerical task routes, checkpoints and factual observations are retained.

## Questions and alternatives

1. Can a supervised interface on the actual recurrent owner bind actor, target
and modality, including directed combinations withheld from teaching? Compare
ordered input against the same architecture with token order erased, an exact
independently housed copy, and twice the ordered training exposure. Ownership
alone earns no positive-transfer claim; a copied frozen core should be equivalent.
2. Can a learned initializer reduce the work of continuous completion through the
existing acquired circle and action-response operator? Compare uniform proposals,
uniform plus refinement, learned proposal alone, learned plus refinement, a 33x33
grid and an analytic least-norm control. Do not infer speed from point counts.
3. Do missing attributes, event queries, role reversals, correction, interruption
and restoration preserve evidence and dependency identities?

## Fixed language study

Four supplied entity names: orbit, beacon, marker, anchor. Four supplied meanings:
can reach, imagine reaching, did reach, must not reach. Eight supplied productions
teach active/passive wording; entity occurrences and role supervision are supplied.
Withhold orbit->beacon and beacon->orbit from every teaching production. Evaluate
these directed pairs separately from a withheld surface composition (please plus
active wording). Unknown words and ambiguous pronouns stay unresolved. This is
finite role-to-constraint acquisition, not discovery of English grammar or ontology.

One engineering pilot seed 2499, 200 updates, teaching scores only. Final seeds
2501/2502; ordered 200, order-erased 200, ordered 400 Adam updates, lr .003,
full teaching batch, gradient norm <=1. Final outcomes are inspected once after
all fits. Selection for the usable route is by teaching loss among the 200-update
ordered fits only, subject to exact teaching interpretation. Extra exposure cannot
silently replace the primary model. Save optimizer, RNG and intermediate tensors
every 50 updates. No old tensor is updated. A frozen shared core is not improved eta.

## Fixed continuous study

Two latent continuous controls in [-1,1]; the two-step clipped recurrence uses the
actual owner's learned action-response coefficients and circle geometry. Continuous
control interpolation is an explicit additional assumption: prior paid controls
were discrete. Seven static parameter quadrature branches retain their parameter
across both steps. Squared distance and avoidance meanings are supplied constraints,
not learned universal physical semantics.

Teach a 5->32->2 initializer from 4,096 conditional examples generated by the parent
operator (seed 25100), 400 Adam updates of 128, lr .003. These are imagined teacher
examples, never independently observed facts. Do not grant physical calibration.
The analytic least-norm inverse supplies targets within an unclipped local family.

Final: 256 cases from each of ordinary, narrow, unreachable and omitted-torque
conditions (seed 25200), crossed over inference methods. These are correlated
case-method outcomes under one parent, not 1,024 independent physical worlds.
Eight controls per proposal cloud, at most 12 normalized projected gradient steps.
Report nominal endpoint residual, assessment-only changed-law residual, coverage
at 0.03m, query counts, gradient batches and wall time. Omitted-law outcomes cannot
enter the solver. A grid provides a diagnostic witness, not a general impossibility
certificate. New language errors and wrong-model outcomes remain in raw results.

## Lifecycle and decision

One owner must serve the new interface, the old numeric tasks, recurrent path and
motion generator. Gradients must reach the intended new tensors through the actual
shared computation. Preserve all predecessor tensors exactly. A conditional search
must not add observations. Store branches with model, language, premise, source,
units, static parameters and control state; resume by replay. Reject altered/stale
state rather than silently rebinding it. Missing beacon position requests the
location interface; three qualified local simulated sensor readings estimate it
and reopen the pending request. Missing actor dynamics remains missing. Historical
occurrence requires event evidence and cannot be answered by a feasible completion.

Accept only as a conditional local research tool if contracts pass and the learned
route improves some measured task outcome beyond dispatch. No general capability,
quantum advantage, applicability calibration, learned investigator or positive
cross-route transfer claim. Keep the classical mainline. EqProp, tensor/Born and
mixed controllers remain candidates until this dependency works.

All numerical work uses the existing one-thread / 2 GiB supervisor and remaining
562.8056s allowance, including the v10 replay. No new resource grant is inferred.
Static research, hashing, extraction, coding and documentation have unmeasured
engineering cost; they are not reported as zero. Freeze source identities with
each run. Preserve defects and failed receipts; numerical changes require a new
identity. Use fresh output directories and never retune against final outcomes.
