# Reconciliation and addendum map

I started from local SERA `29f50f92bf29dbbd74b22f80d4195a3a98a203b1`
and Kavi `50f743cc44794b67bc1d30b927e25991197edce2`, both clean before
this work. The addenda's published SERA reference `9104839` is historical.
The local v5 integration, GG-GUARD-002, task-transfer checkpoints and all three
existing store pointers take precedence. The new work uses an additive package
and descendant store; no earlier source implementation or store was replaced.

`reconciliation.json` records full archive hashes, every extracted file identity,
HEADs, pointers and the starting numerical ledger. The initial owned lock was
absent. Process enumeration by WMI was denied; the non-invasive Get-Process
fallback found no visible Python process at reconciliation. No process was stopped,
no service restarted, and no job was assumed safe to kill. Subsequent numerical
jobs used the existing exclusive supervisor. Resource authority remains the
previously approved allowance, initially 562.8055745 seconds remaining.

| Packet | Local disposition | Relevant lesson retained |
|---|---|---|
| v5 Compact | Same original ZIP as the completed local intake; existing unpacker and 777-file verification retained | Actual local owner/inquiry work supersedes the standalone prototype |
| v6 | All 198 manifest entries match | Supervised role binding; symmetric motion can hide wrong roles; sparse top-1 routing and broader worlds fail |
| v7 Light | All 633 delivered manifest entries match | Ordered generated weights can work; rotation cannot change spectrum; anchoring has costs; large datasets/predictions are explicitly omitted |
| v8 | Two text files preserved; no scientific model replay exists | Weight substrate, proposal/belief distinctions and branch-local imagination are design proposals |
| v9 | All 321 manifest entries match | Joint dependencies and static mechanisms; coherent/dephased near tie; omitted mechanisms defeat confident identification |
| v10 | All 245 manifest entries match; five-stage local scientific replay passes | Compatibility settling works conditionally; normalization, compression and a confident incomplete model do not establish truth |

The v5 transport/unpacker was already inspected and executed in a fresh directory
in release 24. It was not run again. v6–v10 are standard ZIPs, including v7 Light;
there is no new compact unpacker to execute. I checked Windows path containment,
collision and link conditions, then extracted to the fresh
`research/intake/v6-v10-20260916` directory. The original six ZIPs remain unchanged.
v7 Light cannot supply a complete archived replay because the omitted arrays are
actually absent. I did not generate replacements and call them archived evidence.

I read the v10 continuation brief, start/status/evidence files, report, theory and
C00–C11 cookbook; the preceding addenda's reports, operating contracts and relevant
briefs supply the comparison above. Embedded prompts are research instructions
within the user's adopted task, not authority to change permissions or resources.

Local v10 replay: 56 regression cases; 15 models/45 prediction arrays; 26,880 grid
records; 11,520 continuous records; 225 transport endpoints, 18 tensor variants,
576 conditional checks and 20 capacity episodes. The exact executed receipts live
in `v10-replay/`. The largest independently reconstructed neural difference was
9.89245e-7 and continuous array difference 3.57628e-7, within the packet tolerances.
No source or tolerance was changed. Python 3.12.14, NumPy 2.5.3 and SciPy 1.18.1
differ from the packet's original environment; Torch is 2.10.0+cpu. Replay is not
new training and does not multiply the original cohort's independent cases.
