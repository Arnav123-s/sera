# C01/C02 architecture audit against v12 and v13

I reconciled both addenda with the actual local owner before extending it. The immutable source packets, SHA-256 identities and original source hierarchy are in `reconciliation.json`. The public SERA pin is 9da1b21b06da4504d7d2b83777d69e874d4caeff. The actual Kavi HEAD is eb5139c2aa3bf6b4fcee39e585231e7b902e539e; the older 50f743 pin remains historical. Kavi was clean and I made no Kavi changes. Stage 27's live revision, checkpoint, corrected operators, research goals and completed evaluations remain intact.

| Adopted requirement | Concrete connection and evidence |
|---|---|
| v13 C01: extend actual StudyR1 | `RefinementSession` restores Stage 27, attaches a `ConceptStudyR1` class to that same object, registers learned coefficients as `nn.Parameter`s, then rebinds guarded knowledge. Exact object identity is asserted across the study, request, typed and neural solver paths. |
| C01: records do not replace learned substrate | Predictions read the registered coefficient tensors. Parameter hashes and model-contract registry reject changed weights, time constants, model classes or evidence. The record carries assumptions and support; the tensors execute the learned force model. |
| C01: preserve exact polynomial route | `exact_motion` calls the retained `StudySession.motion`. The result is marked CERTIFIED_ALGEBRA with empirical premises separate. Every earlier tensor is compared byte-for-byte after learning. |
| C01: branch-safe subject memory | Observations have subject, source, clock, units, sensor availability and evidence kind. Models retain predecessor versions and temporal-state meaning. Imagination is read-only; it cannot pass the observation intake contract as factual evidence. |
| C02: test existing R1 first | The restored model has width256, eight heads, memory dimension32 and gated delta memory. Frozen-memory, conditionally adapted-memory and reset-memory arms use the existing `shared_step`; all conditional updates are registered on that owner. |
| C02: strong controls | Eight-step window MLP, 32-state GRU, coarse/instantaneous equations and seven fixed relaxing-kernel candidates. Coefficients are learned; feature choices, kernels and selection schedule are supplied. Two optimization seeds, same input channels and exposure schedule. |
| C02: same present, different histories | Pairs end at the same h=10 and v=-.3 with the same future command sequence. History-conditioned separation is scored against independently simulated consequences. |
| C02: no privileged latent inputs | The feature boundary exposes h,v,u, availability and dt. Hidden force, family labels, coefficients and future targets are assessor-only. A regression changes those assessor fields and checks input equality. |
| C02: noise and missingness | Clean, noisy and 10%-missing views are scored separately. Fitting excludes transitions with missing velocity; imputed history and noisy finite differences remain explicit engineering approximations. |
| v12: body/source distinctions | Subject identity, normalized commands, units, causal observation order and source identities are required. Synthetic data stays labeled SYNTHETIC. Separate-subject changes are tested for interference. |
| v12/v13: omitted mechanisms and uncertainty | Quadratic drag is a prospectively specified challenge. Validation residual is a diagnostic, not a calibrated guarantee or proof that a physical model is complete. Varied controls are required for coefficient identifiability. |
| Existing applicability | All parent parameters and guarded knowledge are preserved and rebound. The new route has its own finite empirical admission contract. GG-GUARD-002 is neither replaced nor treated as a calibration certificate for this new family. |

The physical runtime is a parameter-estimation integration with supplied observations and equations. It adds learned executable distinctions when its measured validation criterion demands them. The neural controls measure what the old memory learns under this finite curriculum. Sharing an owner does not by itself establish positive transfer, learned variable discovery, an improved acquisition policy, or subjective experience. Those claims require their own evidence; this release reports the capabilities executed here.

I found and corrected F01 before opening final evaluation: the intended simple teacher had a nonzero initial decaying force. Original sources, fits, optimizer state, comparison results and costs are preserved separately. This repair changes the teacher to match the frozen contract and does not tune against final examples.

The relevant source-packet verification is distinct from device integration: v13's isolated FastMap fixtures are not restoration of the trained owner. The device audit restores that owner explicitly. Strict cross-runtime equality failures in the supplied neural and physics results remain recorded, alongside independent numerical checks; no packet tolerances were silently weakened.
