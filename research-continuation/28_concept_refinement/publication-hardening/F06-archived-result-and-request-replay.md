# F06: qualify the measured last-bit differences in retained readers

The complete branch diagnostic found one difference in an archived, stale conditional branch: its seven-term float64 energy was `0.0008018090864610274` locally and `0.0008018090864610273` on Linux, a difference of one representable unit. The stored starting points, full refinement trajectory, selected controls, parameter endpoints, distances, decision and evidence qualifications matched. Two saved English request confidence scores differed by one float32 unit; their complete intent labels, entity spans, tags, source identities and training counters matched.

I retained the failed remote runs and added two limited comparisons. Conditional energy must be finite, nonnegative and within eight representable float64 units, allowing final-bit variation in the seven-term sum. Every other conditional-result field remains exact. Request confidence uses the previously declared float32 score bound, while every other frame field remains exact. Neither reader changes the stored answer, replaces a starting point, updates a learned weight or changes the applicability gate.

The [PyTorch numerical-accuracy documentation](https://docs.pytorch.org/docs/main/notes/numerical_accuracy.html) explains why operation order and platform kernels can change the final bits. That documentation is context for measuring the discrepancy; it is not a blanket exception to verification. The concrete observed values, unchanged owner hashes, rejected semantic mutations and independent trajectories determine this narrow repair.

Both predecessor readers are preserved under `pre-portability-source/`. Each successor admits only explicitly listed source hashes while its own reader bytes match the reviewed hash. Restored records retain a digest of their original source record and do not mutate their input. Exact current-source round trips, unreviewed-source rejection and malformed, nonfinite or excessive numerical changes are tested. The publication receipt records final local and remote outcomes. These are persistence repairs; C01/C02 learning, selected models and completed evaluations remain frozen.
