# F05: retain exact conditional branches across neural proposal roundoff

Matching the local instruction profile restored exact factual-owner checks on both tested CI workers, but the constraint initializer still differed by one float32 unit. The source, selected roles, physical model and random half of the initial cloud matched. The recorded original starting points, refinement trajectory and conditional results remained internally exact. The profile adjustment is retained alongside this measured residual rather than described as sufficient by itself.

The successor reader validates three distinct objects separately. It compares executable role/mode binding and physical-model fields exactly. It compares only the uncalibrated binding scores with the already declared float32 score bound. For the learned proposal it requires an 8-by-2 finite bounded cloud, an exactly reproduced random half, the exact four-point supplied jitter construction and float32-representable centers; the learned center may differ by at most float32 epsilon. It then replays the original stored starting points and requires exact intermediate points and results. The reader does not replace the starting cloud, recompute a different saved answer or update any weights.

Only the authenticated predecessor runtime `74abc339e58b074b32468270895a7f6dac66f35bdc63344df7cc6397b7bfe5d9` is admitted by the exact reviewed successor. Each successfully reverified branch retains its predecessor source and record digest; its interpreter/dependency references are explicitly rebound. The session records the original complete-record digest in its migration history. Input records and files remain unchanged, and current-source reloads do not add duplicate migration events.

Sixteen targeted tests passed, covering the measured Linux differences, unchanged executable bindings, random-sample and jitter tampering, out-of-range/nonfinite scores, an excessive coherent proposal shift, unreviewed reader code, exact owner preservation, unfinished progress and current-source round trips. Full regression and remote qualification results are recorded in the publication receipt. This is persistence engineering; it does not alter the completed C01/C02 learning or final evaluation.
