# Exact continuation

Continue `runs/sera-acquisition-live`, owner `e127e23509a9134cdc38dfd77be641be66ce2b82063d4e6aefd9a06dd0462636`. Do not repeat the completed Stage 34–37 studies or change their final cohorts. The full parent chain, verified sum corrections, discovered rules, reconstruction settings, RNG and quest history are saved.

Immediate executable action:

```powershell
.venv/Scripts/python.exe scripts/run_acquisition_bounded.py --seconds 30 --output runs/next-acquired-route-001 --module experiments.autonomous_discovery.acquire -- use --coefficients 2
```

The next research cycle should generate a larger frontier from retained rules, choose new prerequisite gaps, and compare self-selected investigation with exhaustive coverage under the same complete cost. Keep the degree and operator-domain guards, require independent certificates for composed scope changes, and distinguish useful scoped consequences from a new generating relation. Retain the strongest existing control. Residual-assisted acquisition remains explicitly supervised by the known mathematical contract.

Read `runs/v3-batch-001/budget.json` before numerical work. Published balances are snapshots, not grants. Use a fresh output directory and the owned one-thread/2-GiB supervisor. All earlier stores and rejected attempts remain preserved.
