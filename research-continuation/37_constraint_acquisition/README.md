# Discover, acquire the missing prerequisite, and return

[Home](../../README.md) · [Results](report.md) · [Protocol](PROTOCOL.md) · [Preceding gap](../36_autonomous_discovery/report.md)

SERA derived three missing summation basis cases from retained mathematical constraints, returned to its self-selected investigations, and added **eight further certified relationships**. All **256 alternative-route executions** on 32 fresh polynomials matched independent calculations. Original mathematical successes and 202 protected tensor records were retained.

The current learner is `runs/sera-acquisition-live`. Use its first discovered discrete-accumulation route:

```powershell
.venv/Scripts/python.exe scripts/run_acquisition_bounded.py --seconds 30 --output runs/my-acquired-route-001 --module experiments.autonomous_discovery.acquire -- use --coefficients 2
```

Coefficients are rational, constant term first. For input 2 the discovered double-summation route returns **n²−n**. Its result is independently checked before being returned. The command uses the prepared local workspace, its live allowance, one CPU thread and the 2 GiB process-tree cap. Choose a fresh output directory.

`python -m scripts.acquisition_artifacts` verifies the public archive, reconstructs all four checkpoints, independently checks the acquired basis cases and checks the discovery certificates. The private assessor remains local. All earlier learners, their task state and qualifications remain preserved under their original owners.
