# AC-001: return to an internally identified gap

Continue the completed Stage 36 owner and preserve its zero new-discovery result, all failed executions, reward state and parent. Its investigation identified a retained primitive that fails prerequisite checks. The generic acquisition controller reads the unconnected primitive names from that saved state; I do not provide a target identity or select a missing output.

For each such operator, assess the representation basis through degree five. For an incorrect result, use executions of the retained integral to generate candidate directions. Query the existing operator constraint on the current proposal and its perturbations, fit coefficients to its linear residual, and verify the constructed candidate with both the original coefficient checker and the independent evaluation/identity checker. This is automated supervision from known mathematical constraints, explicitly including residual feedback; it is not an unsupervised experiment or pass/fail-only guessing. The generic fitting, representation, existing contracts and correction procedure are supplied engineering. No new target outputs, completed identities or external sources are supplied to the learner.

Correct only certified basis rows on the actual owner and maintain consistent sufficient statistics. Preserve its prior model and every already correct basis case. After acquisition, the continuing learner independently chooses a fresh three-depth frontier using the existing procedure and new imagination seeds. Previously completed attempts remain in history. Exact scoped certificates and composed-closure deduplication control discovery credit. Reconstruction settings may improve only through the existing independently verified procedure.

Freeze discovery before 32 fresh integer-coefficient polynomials from seed 37991. Measure original-versus-updated operators on identical inputs, execute new routes, count withheld cases separately, and make no learning updates on final cases. Replay exactly from the second post-acquisition checkpoint. Keep all other protected tensors and the source gate. Retain the actual acquired state and a concrete command for using a new route. Original task and prerequisite knowledge remain recoverable through the parent chain.
