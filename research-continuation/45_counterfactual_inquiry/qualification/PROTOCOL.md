# CI-002: challenge apparent completion before qualification

CI-001's full 31-question validation found wrong exact-consensus predictions for every selector. Entropy and balanced selection each produced 24 wrong cases, spread 16, and random/learned selection eight each, among 216 admitted validation queries per selector. Every affected question used a cubic companion omitted from the initial grammar. Several procedures stopped after one or two measurements because one narrow-family world survived. Their confidence did not test the missing family. All original source bytes, trajectories, costs and the 57-question experimental owner are preserved. The 171 final questions have not been opened.

This additive successor changes the completion contract. Before closing an investigation, challenge the current survivors with the full available retained power basis, including powers 3 and 4. Commit the additional worlds, replay existing observations against them, and use the remaining observation allowance to separate any new alternatives. Keep the same total ten-attempt budget. Even when that budget is exhausted, the answer must include every newly compatible alternative instead of certifying the earlier narrow consensus. No concealed equation or new source answer enters the proposal interface.

The original question inventory, partitions, simulator, noise, five comparison methods, assessment probes and selection criterion remain unchanged. Development questions are necessarily replayed under this changed closure contract; original outputs remain under CI-001. A new owned procedure head is trained from the repaired practice trajectories. The earlier head remains registered and archived. Start the successor from the actual latest experimental owner; preserve its independently proved conditional rules and original checkpoints. Evidence-compatible belief weights are revised by the new observations, with both versions retained in separate stores.

The successor uses `experiments/counterfactual_challenge.py` as an explicit process-local adapter around the preserved loop. It does not edit the predecessor's files. Freeze the adapter, driver, protocol and predecessor pointer before the full repaired practice/validation runs. Deploy only after passing the same validation gate. Then open the untouched 171-question final partition once, with fixed procedure weights and selection. Apply the original proof, symbolic-background transfer, exact-owner restore, retention, source gate and credit checks.

This tests whether broader challenge resolves the observed early-stop failure in the declared family. Counterfactual identities, noisy simulated measurements and supplied explanatory names remain separate. Preserve unresolved alternatives; points do not substitute for qualification. All elapsed costs, including rejected CI-001 validation, remain charged under the same one-thread, 2 GiB, no-paid-service policy.
