# Local reconciliation

The SERA checkout started clean at `20e836e702102cf531d641c2142e578e76e2f65d`, after the completed solution-portfolio publication. The actual current parent is `runs/sera-solution-progress-live`, revision 2, owner `1d376d044ddba522401da96e6c36475b3d57cde73ba97358cfc33a502ba4f6f9`. Its exact pointer, 609 tensor fingerprints, 217 routes, prior questions, eight empirical models, taught-source bindings and applicability gate are in [inventory.json](inventory.json).

The preserved Kavi source checkout at `research-continuation/00_sources/kavi-pinned` is clean at `50f743cc44794b67bc1d30b927e25991197edce2`. It is a pinned source snapshot. This continuation changes the SERA owner and adds new files; it does not modify that Kavi snapshot or any completed Stage 44 source or evaluation.

The initial process inspection found no matching active SERA/Kavi numerical worker. New jobs acquire the existing exclusive research lock and release only their own lock. The resource ledger records the user's later removal of the elapsed-time limit; older minute balances remain historical. One CPU thread, the 2 GiB process-tree cap and no paid services continue to apply.

The user's pasted discussion was read as discussion to evaluate. Its main diagnosis matches Stage 44: the earlier queue asked for missing quantities, so its search did not itself generate intervention/threshold investigations. The engineering attribution is corrected here: the guard construction, question grammar, novelty rules and checking machinery are implemented procedures; fitted coefficients and updated value weights are learned state. The requested broader behavior is implemented additively rather than attributing an untested general limitation to the learner.

The first inventory job encountered an original dictionary entry longer than the existing binding interface permits. Its failure and cost remain preserved. The additive collector now records that exclusion and keeps the original source gate intact. The successful inventory admitted 26 previously taught physical meanings. No dictionary source was truncated to force admission.

The first eight development questions and all their original source bytes are preserved under `runs/CI-study-001/development-v1`. They belong to the predeclared development partition. Final evaluation questions have a separate deterministic partition. The complete prospective [protocol](PROTOCOL.md) and [freeze](freeze.json) identify the experiment that follows.
