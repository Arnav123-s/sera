# CI-001: curiosity, counterfactual experiments and reusable explanations

## Reconciled starting point

Continue the Stage 44 reconciled shared owner `1d376d044ddba522401da96e6c36475b3d57cde73ba97358cfc33a502ba4f6f9` at publication `20e836e702102cf531d641c2142e578e76e2f65d`. Preserve 609 tensor records, 217 solution routes, earlier reading/languages/memory, and the closed novel-definition gate. Inventory and the generated frontier identify their exact source bytes. The current language bindings supply familiar names; no new physical target equation or explanatory source is supplied during conjecture.

## Prospective question and mechanism space

The learner's executable dependencies generate all 228 eligible ordered variable questions in its four retained motion/polynomial domains. Its 21 retained forward/inverse parameterizations provide alternative explicit control assumptions. The agenda alternates underexplored domains, then prioritizes questions with more compatible parameterizations. Completed question identities are suppressed. These domains and generic control grammar are inherited/engineered representations, not independently invented fields of science.

For each question, exhaust the initial grammar before feedback: change one available control by a positive multiplier; hold the others fixed, or couple one companion through multiplier powers -1, 1, 2, or reversal 2-z. Use the actual owner's learned operator and inverse coefficients. Preserve complete imagined worlds, domain guards, mechanism assumptions and aliases. Check limits, roots and stationary points as conditional consequences. Never interpret a singular endpoint as an admitted physical state. Finite summation retains integer time.

The learner asks about the original target first. When target predictions agree but explanations differ, it selects another observable that can distinguish them. If every initial explanation is contradicted, it expands its generic power basis through 3 and 4, commits the enlarged portfolio, reapplies its existing evidence, and returns to the original question. Ten observation attempts per question are the declared finite comparison budget. This is an experiment boundary, not a time allowance or a claim of exhausting possible science. Preserve unresolved discrepancies with a precise continuation request; stop revisiting a saturated question without changed evidence or representation.

## Concealed evidence and comparisons

After proposals are committed, a separate assessor selects a concealed structural mechanism from the supplied simulation family. One third of question identifiers request a cubic companion outside the initial grammar. Selection is deterministic from the question digest and recorded seed. The assessor uses independently implemented domain equations and inverse solving, not the learned predictor. Bounded uniform measurement error is 0.002*(1+absolute baseline observable). Repeated queries share their measurement and cannot farm independent evidence. All records identify simulated observations explicitly.

Compare entropy, numerical spread, balanced probe coverage, random selection and a learned linear verified-progress predictor on the same questions, hidden mechanisms and observation limits. The owned eight-feature predictor learns ridge sufficient statistics from replay-checked practice information gain, with -1 for an invalid or contradictory attempt. It receives no concealed mechanism labels or sealed assessment targets. Candidate beliefs are finite 0/1 compatibility weights updated from measured intervals; empty sets trigger expansion and are never normalized into false certainty.

Partition question IDs before study: hexadecimal first eight digits modulo eight equals 0 for procedure practice, 4 for validation, and all other values for final evaluation. Earlier eight implementation checks used only IDs divisible by four. Every question in each partition is run, rather than a selected demonstration subset. Train on random practice trajectories; freeze the learned coefficients before validation. Choose a deployment selector from validation by zero wrong consensus, most correct consensus, coverage, completion, then fewer observations. Keep learned and fixed controls even when a fixed control wins.

Eight distinct held-out multipliers per question evaluate the original target. Report admitted cases, truth inside the surviving prediction envelope, exact correct consensus, wrong consensus, normalized median prediction error, costs, expansion and unresolved questions. Final comparisons are opened once. They never train the policy, select hyperparameters, determine rewards or supply the learner's answers. Final belief updates use only the predeclared observation interface.

## Deeper reusable knowledge and credit

After each complete investigation and independent replay, examine surviving mechanisms for response ratios independent of arbitrary symbolic background values. The proposer uses retained operator/inverse coefficients; a separate structural checker proves the proposed normalized relation for all admissible backgrounds. Retain original mechanism, nonzero initial-target condition and both-world guards. This symbolic check distinguishes a reusable rule from a coincidental curve at one context.

Store compatible-world weights, compact normalized-rule coefficients and the learned procedure head on the actual shared R1 owner. Preserve all previous tensors byte for byte. Award one point for the first independently verified, nonconstant normalized shape of a derived outcome. Controlled outputs, constant outcomes, repeated shapes, aliases, extra prose and extra candidates earn no points. Preserve additional valid scoped routes without manufacturing novelty credit. Bind every reward to the original goal, committed predictor, source, proof and replay. Conventional words are attached only after verified completion; they are explicitly supplied explanatory feedback.

## Verification and preservation

Freeze implementation, protocol, parent pointer, inventory and frontier before full practice. Save complete conjectures before source access and each chosen probe before its observation; checkpoint each evidence update. Reproduce selected trajectories independently, check all their conditional alternatives, and test interruption/resumption, stale/duplicate credit, forged feedback, guard preservation, parent retention and actual-owner identity. Raw failures and development variants remain archived. Exact checkpoint restore and portable artifact replay are integration gates.

Local work uses the user's standing unlimited-time authorization, one CPU thread, 2 GiB committed process-tree memory, no paid service, and a scoped keep-awake request during owned jobs. Record every job's wall time, memory, exit status and source identity. Earlier completed experiments and running work from other sessions remain intact.
