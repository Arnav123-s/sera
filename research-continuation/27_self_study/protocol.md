# SS-001 / SS-002 frozen integration protocol

Frozen before new model execution on 2026-09-17. Parent: the preserved request
owner selected in SC-005; exact request-session revision in parent-request.json.
The supplied v11 laboratory is replayed once, separately from these new results.

SS-001 extends the actual RequestSession / shared-owner successor to the original
autonomous_round. A failed exact goal certificate triggers bounded acquisition.
An engineered MathML/TeX reader extracts the first six odd-power identities from
Knuth, arXiv math/9207222v1, and changes endpoints from 1..n to 0..n-1. Source
prose never executes. Each source identity and assumption remains in the event.
Exact rational coefficient checking and independent degree-bounded evaluation
must agree. Rejected source claims stay recorded. A source can be read once per
episode; source bytes are cached and acquisition costs retained.

Each of two registered 13×13 float64 operator maps belongs to the existing
parameter owner. Ridge=1e-6; proposal rationalization denominator<=120. The map
learns from independently verified source examples; an unchanged map, read-only
acquisition, unchecked-source learning and an installed symbolic solver are
controls. All use supplied rational polynomial representations. Reading policy
is fixed goal-filtered random order, seed 2701, at most six lessons. No learned
eta claim. Existing applicability gates and legacy tensors remain intact.

Original goal: sum 2k+3k^3+k^5 over k=0..n-1, for all natural n. On the same
lifetime, teach six polynomial antiderivative basis examples (degree0..5) from a
supplied OpenStax power-rule schema, then solve polynomial-acceleration motion
with explicit initial conditions and units assumptions. Strong installed algebra
is a privileged zero-reading reference, not a learned capability.

Development: 32 new odd-polynomial combinations (degrees1,3,5,7,9), 32 degree<=5
integral combinations, seed2702. A defect repair may replay development after
preserving the failure and old source. Final: 128 of each plus 64 composed motion
tasks, seed2791, coefficients -7..7, accessed once after code and candidate freeze.
Retest the original sum following integral learning, exact interrupted restore,
source-free inference, and retained predecessor parameter bytes and fixed request
outputs. No extrapolation from finite physical simulations to unrestricted laws.

SS-002 teaches Spanish, French and German real request interpretations. Each
locale uses its first1200 valid official training rows, the first160 valid even-ID
official-development rows for development and first160 odd-ID rows for final.
Official test rows are unused. Prior English semantic IDs can overlap across
translations: measure language acquisition, not new semantic-domain transfer.
The previous MASSIVE final cohort is never scored again or used for tuning.

Two fixed arms, seeds2711 and2712: current-language updates versus updates with
a256-record reservoir from each already-taught language and English. Three
successive languages es-ES, fr-FR, de-DE; 400 Adam updates per stage, batch32,
lr0.001. Half each batch is current data; half is either current repetitions or
reservoir samples, so total presentations match. English reservoir comes only
from existing training data. Source/sample RNG and Adam state persist through
all stages. Protect every non-stream tensor, including acquired study maps.
Development selects a replay candidate by mean new-language intent, requiring
English intent loss<=10percentage points and each new language intent>=35%.
Final once: all arms, frozen selection,160 rows per new language; English
retention uses a fixed160-row development probe, distinct from SC final. Keep
all stage checkpoints, rejected candidates, exposure counts and costs. If no arm
passes, preserve the research candidate and the earlier qualified live interface.

All numerical invocations use the existing one-thread,2GiB supervisor and current
allowance. No original repository/store reset; no other job is stopped. Freeze
source hashes at each completed stage; record every genuine defect repair and
make explicit migrations if a persistent source contract changes.
