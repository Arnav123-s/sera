# SS-003: bounded correction of a rejected mathematical source claim

Register before correction execution or its new final set. The first SS-001
source screen rejected the arXiv eleventh-power table row. Preserve that row and
the five admitted rows. Do not alter the source or retrofit its provenance.

Use the same actual learner, independent checker and original-goal interface.
The supplied proposer changes one coefficient of N^j/6, j=1..6, by an integer
delta in -8..8 excluding zero:96 finite candidates. Check every candidate by
rational coefficient equality and independent degree-bounded evaluation. Admit
only a unique verified correction, labeled a newly derived candidate, not an
additional source authority. Train one new operator lesson and retry p(n)=n^11.

Assess32 new mixed odd-polynomial development tasks (through degree11, seed2801),
then freeze before128 final tasks (seed2811). Preserve source-free prediction and
all earlier source-learned map rows and old request tensors. The proposer,
representation, correction family and certificate checker are engineering; the
retained source-to-operator update is learned by SERA. Do not claim the correction
policy itself was invented or learned by SERA. This is an exact finite-degree
polynomial identity result, not a proof concerning all zeta zeros.
