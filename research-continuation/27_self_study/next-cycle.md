# Exact continuation and next executable action

SS-001 through SS-004 are completed finite studies. The v11 supplied laboratory
was verified once. The multilingual, mathematical and correction final cohorts
are exposed and closed to further model selection. Inspect their retained
records rather than rerunning completed optimizers or retuning against these
final families.

## Use the admitted learner

The default local store is `runs/sera-study-live`. Its pointer and checksummed
revisions restore the actual continuing owner, the selected multilingual
checkpoint, corrected mathematical weights, evidence and parent goals. Existing
request and constraint stores remain intact. The next immediate executable task
is a checked calculation, for example:

```powershell
.venv/Scripts/python.exe scripts/sera_study.py motion --coefficients "2,3,1" --time 3 --position 5 --velocity=-1
```

Read `runs/v3-batch-001/budget.json` and its owned lock before numerical work.
The release's budget snapshot is historical, not a new allowance. Each command
reserves and settles its cost through the supervisor. No asynchronous worker or
scheduled continuation is implied by this document.

## Exact restoration

Keep the existing local workspace whenever available. The live owner depends on
its preserved predecessor checkpoints and source/data manifests from phases23–26;
this contribution is an incremental release, not a stand-alone pretrained LLM.
Use `archive-manifest.json` to verify archive and member hashes. Restore archives
only into a fresh staging directory, compare any destination before copying, and
keep current local files whenever their validated evidence is newer.

`checkpoints.zip` preserves five terminal language comparisons and the failed
100-step checkpoint, with Adam/RNG state and exact source contracts. All
intermediates remain locally indexed in `local-state-manifest.json`.
`data.zip` preserves the selected language records and license. `sources.zip`
holds current implementation bytes; the failure archive preserves the earlier
runtime used in SS-001/002/004. `live-state.zip` retains every revision of the
admitted descendant. `supervision.zip` retains receipts and logs including
failures. No original arXiv paper body is redistributed in those archives.

The admitted request checkpoint is
`runs/SS-rehearsal-2721/step-002000.pt`, SHA-256
`39631cd5fbce7693684ef02fe90f3e2ba9dc09b033cba1679c4ea286d3b6fa24`.
The earlier English parent SHA starts`5ab3bf1e77b0710ea`.
Mathematical weights reconstruct exactly from the retained verified lessons and
must agree with the saved tensors. A known source migration preserves learned
owner identity; an unknown source change is rejected.

Do not invoke the completed trainer with a larger target by editing its frozen
source. A new learning protocol must have a fresh ID, output directory, source
contract and final partition. To revisit an interrupted historical run, restore
its exact source archive and checkpoint, including the explicit path-repair
migration if using the failed100-step state.

## Next justified research cycle

The next integration should connect learned multilingual request fields to a
persistent, reversible local task, beginning with a structured task inbox.
Freeze source-labelled train/development/final task transitions before any fit.
Use the admitted owner as predecessor; compare verified corrections plus replay,
correction alone and unchanged weights with matched presentations and access.
Measure complete state-transition correctness separately from intent accuracy.
Include missing fields, negation, alternate languages, interrupted tasks and
corrections that invalidate a stale plan. Preserve the existing applicability
and evidence gate when routing requests into an existing investigation.

For acquisition-policy learning, retain K_old, K_new, eta_old and eta_new and
cross all four combinations on a new held-out acquisition stream. Compare a
learned selector against the fixed goal-filtered random control used here;
charge source reads, verifier calls, rejected work and selector training. Reward
independently measured capability gain. A larger retained knowledge base and an
improved investigator must be distinguished experimentally.

The RH goal remains saved. Its next research subtask is the Euler-product
zero-free region Re(s)>1 with explicit convergence, logarithm and rearrangement
obligations. The finite geometric-polynomial antiderivative child is complete.
Acquire the next targeted analytic source, define a checkable lemma contract,
assess prerequisite skills and preserve the exact unresolved proof step. The
original critical-strip statement remains unchanged as those prerequisites grow.
