# Internal discovery from learned computation

SERA constructed **five verified operator relationships new to its retained library**. They connect two available operations: its learned integration map and time-weighting. One core relationship has four composed extensions in the finite vocabulary. It then executed **256 alternative calculations on 64 fresh rational polynomials**, with every result matching an independent rational implementation.

The discovery run received no web results, retrieved equations or completed identity bank. I supplied the two-operation grammar, finite search budget, coefficient-fitting algorithm and independent proof contract. SERA generated candidate coefficients from executions of its retained learned weights, stored accepted coefficients as new owner parameters, and used them to execute alternative programs. This is discovery within a declared mathematical representation, with the origin of each component explicit.

## The connection it found

Let I mean integration from zero to t, and M mean multiplication by t. Composition is written outermost first. The first generated relation was:

**II + IM − MI = 0**

Applied to an acceleration polynomial a, this gives the executable alternative:

**I(I(a)) = t·I(a) − I(t·a).**

This connects accumulated motion with a time-weighted integral. It provides another way to calculate displacement from the same retained primitive. For a(t)=2 with zero initial position and velocity, both routes yield **x(t)=t²**. The newly stored coefficients, rather than a hardcoded copy of this displayed equation, execute the alternative.

The other generated relationships were:

- III + IIM − IMI = 0
- III + ½IIM − ½MII = 0
- IIM + IMM − MIM = 0
- III + IIM + ½IMM − ½MMI = 0

These are four composed extensions of the core connection. Their coefficient rows are independent in the finite program vocabulary; the reward records this added representational coverage. This distinction keeps five stored relationships separate from a claim of five unrelated mathematical laws.

## How proposals became reusable knowledge

For each investigation the learner imagined 12 polynomials and executed candidate compositions through its actual retained integral weights. A numerical dependency fit generated rational coefficient proposals. The independent checker then used exact rational arithmetic to verify the relation on every polynomial basis element in the degree-three input space. Linearity of the supplied operators makes this a certificate for the complete stated polynomial space, rather than a claim inferred only from a few successful numerical examples.

The degree guard follows the taught primitive: three compositions can raise a degree-three input to a degree-five input at the final integration, within the previously learned domain. The executor also checks each learned primitive's result. Initial conditions are explicitly zero for the new displacement interface; source and physical-meaning gates remain unchanged.

Each accepted, previously unspanned relation earned one point. Scalar renaming and combinations already covered by the stored coefficient space earned zero new-discovery credit. All subsequent repeated proposals, costs and search decisions remain saved. The nine investigations awarded **five discovery points** and charged generation costs. Three initial visits supplied exploration; later visits sampled the reward-updated search policy. Updates on the initial visits are explicitly reward-weighted imitation.

## Controls, verification and retained state

| Search | Stored independent coefficient rows | Imagined program executions |
|---|---:|---:|
| Reward-updated search, nine investigations | 5 | 600 |
| Exhaustive depth coverage, three investigations | 5 | 168 |
| Random depths, nine investigations | 5 | 480 |

The exhaustive control found the same relationships at lower generation cost and is the efficient default for this small grammar. The learned search and its full cost remain available as a measured challenger. The comparison counts every generated program execution; it does not equate differently sized search budgets.

The full discovery state resumed exactly from its fourth investigation and reproduced the final five investigations, records, policy and RNG. **All 204 inherited tensor records were unchanged.** The original language, reading, mathematics, physical meanings, task state and source guards remain in the retained parent. The new owner is `ce851ec425e517a76d7c31d36d4d9ae39af9cf6ab1a780fc5e6adbfa4a5bb333`.

The preceding Stage 34 local regression passed 493 tests. Three additional discovery tests cover exact certification, wrong coefficients, alias/rank deduplication and grammar/degree bounds. The actual-owner study supplies the full integration, restart and fresh-execution evidence. [Machine-readable results](results.json) · [Costs](costs.json) · [Mechanism references](RESEARCH_NOTES.md).

## Preserved next tasks

The next discovery cycle should use retained relations to generate larger compositions and choose investigations by the value of the remaining gap. It should distinguish a new fundamental relation from a useful composed consequence and compare against the efficient exhaustive control under equal full cost. Fresh source-domain problems need independently defined assumptions and proof/measurement contracts.

The earlier source-reading demonstration also exposed an unanswered country question: its selected sentence discussed revenue rather than naming the country. That complete result and the human supporting sentence are retained under Stage 34 as a specific open correction task. It was not hidden or tuned against completed evaluation data.
