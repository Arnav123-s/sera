# OD-001: discover new relationships through retained executable knowledge

I continue the actual Stage 34 owner. The user requests discoveries new to the learner, generated through imagination, checked in multiple ways and rewarded for useful distinct connections. This is a bounded mathematical discovery experiment: no new empirical observations or claims of priority in mathematics are implied.

The supplied typed grammar composes two primitives: the retained learned anchored integral I and multiplication by the independent variable M. Words have lengths one, two or three. The input degree guard is three: the retained primitive was taught degrees zero through five, and three compositions reach degree-five inputs to the last primitive. There is no supplied completed identity bank. The learner imagines 12 polynomials per investigation, executes the retained operator, fits sparse dependencies among the resulting programs and proposes rational coefficients. An independent exact rational checker proves each proposal on a basis, using linearity and the degree guard. Novel accepted coefficients become registered executable owner weights.

Nine investigations are frozen: three initial exploration visits, then six choices sampled from a three-logit reward-updated policy. A linearly independent certified relation earns one point; scalar aliases and combinations already spanned by retained discoveries earn zero. Each investigation pays .01 per generated program. Every failed, redundant and successful proposal remains recorded. The global record persists through serialization. The initial supervised exploration actions also train the reward policy; they are supplied curriculum visits, not on-policy samples, and any resulting policy change is labeled reward-weighted imitation.

Controls: exhaustive search of all three depths once, and nine random-depth visits from seed 3502. Both use the same actual owner's primitives and the same certificate, preserving the cost and all findings. No superiority claim is made without matched costs. With the learned search, first retain a midpoint after four rounds and independently resume its remaining five rounds exactly. With all search finished, freeze discoveries and evaluate 64 fresh rational polynomials from seed 35991, with coefficient denominators 5,5,2,1 for degrees 0,1,2,3 so the primitive's retained denominator-120 reconstruction remains representable. Compare every generated alternative for targets II and III against an independent rational execution, including nonzero initial-condition rejection at the task boundary. Test high-degree/tail rejection, wrong coefficients, scalar renaming and previously spanned relations.

Save the full parent learner and discovery state. Check all inherited tensors and guarded knowledge, preserve source gates and original tasks, and execute a newly constructed displacement route. A source-sentence selection mistake found during Stage 34's demonstration remains a separate open correction task; it is not retuned against the completed evaluation.
