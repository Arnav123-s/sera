# Continue the actual discovery owner

Use `runs/sera-discovery-live`; do not restart OD-001 or change its completed final cohort. Its parent includes the Stage 34 weights, qualification history and original task records. All discovery coefficients, rewards, exact certificates and unfinished earlier goals are retained. The live resource ledger remains `runs/v3-batch-001/budget.json`; historical balances are not grants.

Immediate executable use:

```powershell
.venv/Scripts/python.exe scripts/run_discovery_bounded.py --seconds 45 --output runs/discovery-next-use-001 --module experiments.operator_discovery.runtime -- solve --coefficients 2
```

Choose a fresh output directory. The next research protocol should derive candidate program compositions from retained discoveries, explicitly distinguish base relations from their algebraic consequences, and reward new independently checked capability coverage at matched search cost. Keep discovery internal to known executable knowledge unless the user authorizes external acquisition. The supplied finite grammar, rational certificate and prior learned integral remain explicit starting knowledge. Preserve the current exhaustive-search control and all negative/redundant proposals.
