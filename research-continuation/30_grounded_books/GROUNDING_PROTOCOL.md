# GD-001: definitions become conditional motion inputs

I freeze this protocol before training the binding heads or opening the new College Physics final definitions. HB-001's completed language finals remain closed and its next-word heads remain experimental: the unigram control had lower loss. This study tests a different, explicitly typed capability.

## Sources and partitions

Teaching uses original Webster entries/sense spans and OpenStax Physics glossary meanings. Every span retains the complete entry or original XML identity. The engineering vocabulary is position, velocity, acceleration, force, mass, and other. Human headwords/glossary terms supply labels through an explicit term map; these labels are not independently discovered concepts. Definitions, not headwords or source IDs, are model inputs. Do not silently label displacement as absolute position, angular velocity as linear velocity, or arbitrary uses of mass/force as mechanics.

Split OpenStax teaching definitions by complete normalized term within each class, deterministic SHA-256 order: reserve one quarter, at least one, for development when a class has at least two terms. Preserve at least one teaching term. Cap unrelated definitions at 80. Add curated original physical dictionary sense spans and nonphysical spans to teaching. Keep complete raw entries alongside selected spans; use no generated paraphrases. Final definitions come from the first 60 modules in the publisher's College Physics collection at commit fd1b25dfd5d8c6580c6e2b2b34a19e29cc69ada9, retrieved only after freezing this protocol. Exclude exact normalized definition overlaps with teaching/development and cap other at 40. Report remaining near-source dependence: both textbooks are OpenStax, and some upstream prose was encountered by previous reading stages. This is a new wording/source transfer probe, not a claim of independent author sampling.

## Finite model comparison

Restore the actual Stage 29 owner and retain HB-001's selected language delta as experimental state. Attach three six-class readouts to this same BookR1 object. Compare (1) existing R1 ordered definition state plus learned semantic mean; (2) normalized hashed word and bigram counts, 1,024 bins; (3) their concatenation. All old tensors stay fixed. Features use at most 128 tokens and headwords are not prepended. Fit each new linear head with balanced cross entropy, AdamW learning rate .01, weight decay .01, seed 3031. The finite checkpoints are 40, 80, 160, 320 full-batch updates. Select by development macro accuracy, then cross entropy, then smaller head. Include a majority-class control. One final evaluation follows the selected condition/checkpoint; no retuning on it.

Confidence gates use development only: from thresholds 0, .5, .65, .8, .9, .95, select the lowest threshold with at least three admitted physical examples and no incorrect admitted types. If none passes, keep all autonomous bindings tentative. Probabilities are classifier scores, not calibrated truth probabilities. Preserve every alternative. An explicit source span, physical-context assertion, matching SI unit, positive mass, finite time and the named model assumptions are also required. Units filter applicability; they never override a contrary language prediction.

## Conditional execution and correction

An accepted binding routes its numeric value to the existing StudyR1 motion operator: position to the initial position, velocity to initial velocity, acceleration to a constant acceleration, force through the supplied Newtonian F/m relation with a supplied positive mass, or mass through the same relation with a supplied force. Premises name one dimension, constant net force/acceleration over the interval, an inertial frame, nonrelativistic motion and consistent SI units. Numeric values and premises are user-supplied structure, not unconstrained natural-language extraction.

An independent Fraction checker verifies x=x0+v0*t+a*t*t/2 and v=v0+a*t. Opposite-force and doubled-mass branches are checked without mutating observations or learned weights. These checks certify conditional algebra only; they do not confirm the lexical interpretation or physical occurrence. A verified concept record must distinguish human-source interpretation from algebraic certification. A missing concept or incompatible assumptions retains the original task and can invoke the existing arXiv investigation. Retrieved papers remain unverified references and do not enter the human-only foundation curriculum.

Run a finite 60-case rational motion integration cohort per admitted type using seed 3041, plus incompatible-unit, negative-mass, nonphysical-context and missing-premise tests. Generated numerical diagnostics are clearly separate from human language training. Test exact checkpoint resume, all predecessor tensors, existing four-language interpretation, earlier exact algebra and empirical forecasts, snapshot restoration, tamper rejection and a retained interrupted acquisition task. Existing applicability gates remain active.

Only publish supported interfaces with their admission status. Preserve all rejected cases, costs and checkpoints. Use the already approved single-thread, 2 GiB local worker budget.
