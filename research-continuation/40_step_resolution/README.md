# Verified resolution of practice steps

This successor follows the retention-rejection plateau with smaller fractions of the same proposed update. Every accepted fraction passes the original independent gate and makes strictly positive macro progress. [Protocol](PROTOCOL.md) · [Selection](selection.json) · [Final results](final.json) · [Audit](audit.json) · [Report](report.md).

The transaction adapter is in `experiments/refinement_resolution.py`. It retains every attempted scale, the original checker result, the accepted model and its reward identity. The original SR experiment is preserved. The shared final cohort is used only after both sets of training and selections are fixed.

Run the [nine-task example batch](../../docs/START_HERE.md#run-several-tasks-through-the-continuing-learner) to use the latest independently admitted owner. [Example tasks](example-tasks.json) can be copied and edited for attributed reading, language requests, independently checked polynomial calculations and conditional physical imagination.

The first worker was externally interrupted after its 640-decision checkpoint. Its full reservation remains charged; its state, log and exact continuation are retained. [Interruption record](interruption.json). No other job or earlier store was stopped or overwritten.

`python -m scripts.refinement_artifacts --stage 40` verifies the compact checkpoint archive. Original local checkpoints remain unchanged.
