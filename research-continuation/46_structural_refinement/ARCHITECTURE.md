# Physical refinement on the continuing learner

`FieldSession` restores the independently qualified counterfactual successor. `FieldR1.attach` extends that same parameter owner with a registered `physical_fields` collection. Language, taught physical meanings, exact polynomial operators, empirical alternatives, rational solution routes and counterfactual rules keep their existing tensors and interfaces. The baseline and independent replay record their actual identities and outputs.

## From a question to an acquired model

Each investigation retains its original goal, typed state and evidence source. Its initial answer is calculated before the twelve additional probes. Two candidate descriptions remain available: a radial potential and a potential whose stiffness depends on direction. Their coefficients start without the teacher's parameters and change through observed acceleration pairs.

The continuing owner executes its learned integral on the linear and cubic monomials. The verified results supply the quadratic and quartic factors of the potential. The force layer stores fitted stiffness, nonlinearity, damping and rotational velocity coupling as executable weights. Sufficient statistics retain the information required for further updates.

The investigation controller chooses a state by candidate disagreement and design variance, commits that choice with the goal and predictor identities, and only then obtains its observation. A balanced geometric controller is the prospective comparison. Both receive the same finite observation allowance. These controllers and candidate forms are supplied engineering; the measured coefficients are acquired by SERA.

Separate evidence selects between the candidates and checks adequacy. Selection requires a meaningful improvement before relaxing the radial constraint. An independent NumPy implementation reconstructs the fitted coefficients from the acquisition data, evaluates the reserved evidence and checks the energy assumptions. An inadequate family retains its alternatives and an open original goal. It does not gain factual status from agreement among its own predictions.

## Branches, plans and explanations

A physical view binds one subject, model object, complete numerical state, evidence revision and qualification. New evidence, altered weights, replacement objects or changed qualification invalidate the view. A new view rechecks its receipt against the current weights.

Conditional prediction evaluates the force at a supplied state. A trajectory preserves one mechanism for its entire numerical integration. Planning compares five finite constant-input branches against a target and returns a model-based choice. Explanation reports which candidate survived the independent comparison, the original prediction and the supporting observations. The plan interface executes no external action.

Empirical predictions retain units, assumptions, provenance and assessed state region. Exact retained polynomial calculations continue through their independent algebra checks. Neither result changes the evidence grade of the other.

## Actual R1 comparison and protected knowledge

The recurrent control uses the existing `sequence_encoder`, `initial` and `shared_step` operations. It constructs memory from the 24 acquisition observations. Query states read that memory without their target accelerations, and a ridge readout is fitted using the charged labels. This tests the current representation in this task; it is a distinct control from fitting the structured force weights.

The strongest matched structured control independently fits the always-directional model using the same 24 acquisition pairs. A radial control receives all 72 acquisition, selection and adequacy pairs. The original twelve-pair radial fit remains frozen for the before/after comparison. Reports keep these controls alongside the selected route.

Inherited tensor equality establishes retention of the parameters. Practical outputs, all 26 taught physical bindings, the 217 route identities and the source applicability gate receive separate checks. Sharing an owner does not stand in for these behavioral comparisons. The final audit also records the added coefficient count, tensor bytes and recurrent state size.

## Persistence and exact continuation

Every chosen probe is saved before its response. Every admitted observation and assessment advances the pending record. Completion commits a new append-only store revision. Restart restores the same pending decision, evidence count, parameters and original goal. The independent replay deliberately interrupts the real owner after a committed probe, restores it, and compares its completed state with an uninterrupted continuation.

Completed study episodes refuse repetition. Duplicate evidence and reused assessment records are rejected. Imagined branches remain read-only. Earlier owners, failed attempts, every comparison result and the original v16 packet remain preserved. The release adds the new records incrementally to the preceding archives.

## C01/C02 contract audit

| Requested contract | Executed evidence |
|---|---|
| Continue the actual latest owner | [Reconciliation](LOCAL_RECONCILIATION.json) pins the qualified Stage 45 descendant, local repository state and live resource policy |
| Acquire executable physical weights through that owner | [Final study](final.json): 72 investigations; 24 fitting observations and separate selection/adequacy evidence per subject |
| Reuse current mathematics and recurrent state | The learned integral supplies potential factors; the comparison uses actual R1 sequence encoding and recurrent memory with a fitted readout |
| Protect previous abilities and source applicability | [Independent audit](audit.json): all 855 inherited tensors, 217 routes, 29 practical outputs and 26 taught meanings retained |
| Separate imagination from evidence | Read-only branch and planning checks passed; duplicate observations, changed weights and stale views were rejected |
| Restore and resume exactly | All 72 episodes replayed; a committed probe resumed 60 remaining observations with no repeated initial evidence |
| Measure the complete footprint | 12,470,552 owned tensor bytes in total, including 44,928 newly added bytes; recurrent working state remains 32,768 bytes |
| Keep original-goal return and evidence grades explicit | The original state, initial answer, qualified answer and independent assessment are retained; empirical prediction, proposed action and candidate explanation have separate result types |

The supplied v16 prototype was used as a reference. Its checkpoints did not replace the continuing owner. The acquired field coefficients, independent classical controls and protected earlier routes are measured in the [report](report.md); the [research notes](RESEARCH_NOTES.md) separately identify candidate learned-update, perception and trajectory-proposal work.
