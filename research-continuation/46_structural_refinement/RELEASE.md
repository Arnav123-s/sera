I integrated v16 C01/C02 through SERA's actual continuing owner. The learner acquires physical force coefficients, tests a simpler and a direction-dependent explanation, imagines trajectories and controls, and returns to its original prediction task after independent assessment.

The frozen study completed 72 investigations across 36 paired worlds. All twelve directional worlds qualified the richer representation, all twelve radial worlds retained the simpler route, and all twelve omitted-mechanism worlds stayed open with their alternatives preserved. On directional worlds, acceleration MSE fell from 0.806441 for the initial radial model to 0.000010887 after refinement, matching strong independent least-squares fitting.

Independent replay preserved all 855 inherited tensor records, 217 qualified routes, 29 practical outputs, 26 taught physical meanings and the existing source gate. All 72 investigations replayed, saved state restored exactly, and a deliberately interrupted acquisition completed without repeating its initial evidence. The extension adds 720 acquired coefficients and 44,928 bytes including sufficient-statistic buffers.

The report separates what-if prediction, finite control planning and evidence-ranked explanation. Candidate forms and investigation procedures are supplied engineering; observations determine the retained coefficients and assessed model choice. All simulated provenance, comparison methods, unsuccessful candidates, packet portability differences, source snapshots and costs are preserved.

Both investigation procedures selected the independently best of five offered controls in all 24 qualified worlds. The full local suite passed 593 tests, lint and seven historical release verifiers; all 33 current-owner example requests completed.

Run `python scripts/restore_current.py` in the documented environment, then use `scripts/sera_current.py` with the 33-task example batch. Start with `research-continuation/46_structural_refinement/README.md` for commands and task fields, and `report.md` for the complete measurements.
