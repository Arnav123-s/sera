# PS-001: complete self-chosen questions with verified solution portfolios

The actual KG owner contains 358 distinct mathematical questions in its prior investigation events: 277 have no applicable retained equation route. These are the queue. No chosen equation, expected answer, physical mechanism label or additional source enters proposal fitting. Earlier imaginary worlds and variables remain inherited supplied representations.

The learner executes its retained integral/sum weights to imagine 256 fit situations and 48 separate self-check situations per question. The two old explicit fitting procedures are matched controls. The additional engineered search enumerates dimensionally compatible polynomial numerators and denominators, total degree one through four, under three column orderings. QR dependencies yield rational coefficients, decoded with denominator cap 720; symbolic simplification only removes algebraic aliases. There is no symbolic domain-law access in proposal search. Every declared order/degree is explored for every question. Source and protocol hashes are frozen before the run. Each complete question is a resumable boundary; unsuccessful candidates and calculation counts remain in its artifact. No independent feedback is revealed until the entire finite queue is durably exhausted. This is exhaustion of a declared grammar, not all mathematically possible answers.

After commitment, a separate assessor substitutes independent domain laws into target*D-N and verifies exact zero, plus a nonzero denominator. An identically zero denominator cannot establish an answer. Each candidate's original question, predictor, commitment, proof and assumptions remain bound together. A polynomial identity certifies a conditional answer; it does not assert an observed physical event.

Retain alternative minimal input/guard routes. Canonical rewrites receive no repeat credit. Novelty is relative to the actual pre-run registry; a familiar applicable route suppresses new-coverage credit. Award one point for a previously uncovered sufficient input/guard route, plus 0.25 for a further complementary route to the same original question. Zero points for duplicates, unverified results, extra prose, confidence or extra algebraic terms. Publish the complete proposal inventory and every grade. Train the additive procedure-value weights only from practice proof credit after search; sealed numerical evaluation does not train any weights. No claim of improved future learning efficiency is made without a separate test.

Freeze the proof-selected portfolio before one 128-case-per-candidate final evaluation. Evaluate literal exact reference executions independently of proposal matrices. Integrate only proof- and final-qualified coefficients on the same owner, with exact old tensor retention, source gate, restore and replay tests. A question returns all applicable answers and their assumptions. Any inconsistent premises retain competing routes and trigger clarification instead of selecting a convenient result. New curiosity uses the same investigate-commit-check-return-credit cycle, with a resumable pending state and no source supplementation before its finite search is finished.

Engineering alternatives: explicit sparse regression, dense regression, implicit rational dependency search and symbolic elimination. The present study compares the first three; symbolic domain elimination is confined to proof checking. [Kaheman, Kutz and Brunton's SINDy-PI](https://arxiv.org/abs/2004.02322) motivates investigating rational dependencies. That paper is an engineering reference, not a source of learner answers.

Resources: the user's latest instruction explicitly removes the local time allowance. One CPU thread and 2 GiB committed process-tree memory remain; a scoped Windows execution-state request prevents idle sleep during each owned job. The finite search continues to exhaustion with exact question checkpoints and full elapsed costs. Preserve all failures and old sealed evaluations. Further explanatory feedback is stored only after the learner's independent proposal phase ends.
