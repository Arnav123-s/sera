# QP-001: verified alternatives on CompletionR1

Frozen before new policy training, development selection or final evaluation. W01/W02 adds one actual-owner quest lifecycle, a learned finite-method selector and independently assessed portfolios. The goal is conditional final velocity with alternative time, impulse and work routes. Physical programs, source/assumption contracts and typed grammar are supplied. New weights learn which checked alternatives to retain; writing the registry or scheduler is engineering.

## Finite teaching

Four valid canonical routes: retained integration, impulse, positive-branch work and negative-branch work. Two defective/unrelated programs and 24 exact spelling aliases test filtering. Distinct derivations and opposite sign assumptions remain separate. Five attempts form an episode; one attempt is reserved for a least-examined alternative. All attempts and failures incur cost. A 12→24→1 candidate scorer is registered on the current owner; inherited parameters stay intact. The lower-level Stage 32 investigator and its independent credit remain a separate action space.

The seventh candidate is STOP, which skips an optional attempt without consuming evidence. The fifth slot always examines the least-visited canonical method. Its fixed choice receives no policy-gradient credit. Training episodes simulate separate acquisition lifetimes; only the persistent live owner uses one cumulative discovery high-water ledger. This distinction prevents simulated training resets from becoming a live reward-farming mechanism. All supplied conditional mechanics exercises remain separate from human-authored reading and factual observations.

Compare verified incremental coverage plus a one-time 0.03 valid-method connection bonus, against repeated-success reward. Reward is reduced by 0.01 times method cost. Coverage and method high-water records prevent repeat, rename and forget/relearn payment. This bounded bonus is not a policy-invariance theorem or a correctness certificate. Live credit cannot accept caller-provided scores. Maintenance may be performed without discovery credit.

Two paired seeds, 3301 and 3302; 300 Adam updates of 64 five-attempt episodes per arm, learning rate .003, entropy coefficient .01, gradient cap 2. Training seed 33001 supplies 2048 task-weight contexts; development seed 33002 supplies 256. Each episode requests two to four input scopes with supplied weights. Fixed balanced and useful-source random controls use the same method access and attempt budget. A greedy complementary-coverage control and the initial head are retained. Selection is by development verified utility, before finals.

Final seeds 33991 and 33992 each supply 256 new task contexts (matched and shifted scope preference). Numerical method evaluation uses fresh rational mechanics inputs, including a separate omitted-drag cohort. Report actual guarded deployment, squared error, correct coverage, wrong answers, costs, canonical methods and aliases. No oracle selection from final answers. Balanced remains default unless the learned arm improves coverage/error and cost under this protocol; improvement here does not promote a general curriculum executive.

## Qualification and lifecycle

Quests preserve the original goal, semantic scope, alternative prerequisite routes, sources, candidate methods, evidence, model identity, finite remaining steps and next action. Points, training credit, exact arithmetic checks, empirical boss qualification and operational permission have different records. One million display points buys no eligibility or resources.

A separate trusted assessor process owns challenge generation, reference answers, attempts and a global alpha ledger. For canonical quest index j and attempt t, allocate .05/[j(j+1)t(t+1)], shared across renamed quest IDs. Use 256 fresh independent checks of the frozen candidate; require a one-sided exact binomial lower bound at least .95 and a retained-capability receipt. Keys and answers are not inputs to the learner. This is trusted-process separation, not an OS security boundary against hostile Python with the same user privileges. Assessment outcomes never train the selector. Counterexamples revoke eligibility; any owner weight change makes qualification stale. Restoration revalidates derived fields and ledger identities.

The actual-owner lifecycle includes a failed incomplete portfolio, practice and a checked update, multiple solution routes, fresh qualification, exact restart, a subsequent owner update and conservative staleness/revalidation. Preserve Stage 32 optimizer/credit state and the original open momentum meaning without bypassing its admission gate. Run inherited retention probes, reward/alias/permission/repeated-assessment regressions, independent replay and the full upstream regression under the existing one-thread / 2 GiB supervisor. All numerical costs, including failed checks, use the live allowance.
