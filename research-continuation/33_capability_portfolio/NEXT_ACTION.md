# Exact continuation

The completed QP-001 study and its final cohorts remain sealed. Current owner: **runs/sera-quests-live**, with the assessor at **runs/QP-owner-audit-001/assessor**. Keep its local issuer key and complete ledger together with the learner revisions. This is the descendant that includes the new on-policy method update and the second retained Stage 32 investigator update. The predecessor completion store remains preserved.

The immediately executable task uses this owner:

```powershell
.venv/Scripts/python.exe scripts/run_quests_bounded.py --seconds 45 --output runs/quest-resume-status-001 --module experiments.quest_portfolio.runtime -- status
.venv/Scripts/python.exe scripts/run_quests_bounded.py --seconds 45 --output runs/quest-resume-answer-001 --module experiments.quest_portfolio.runtime -- solve --input research-continuation/33_capability_portfolio/example-work.json
```

Choose fresh output directory names. Read the live resource ledger first; historical balances are not grants. No current numerical job needs restarting.

The unfinished maintenance quest and the original momentum meaning task remain recorded. The next research package should connect human-source acquisition to a genuinely new executable method: pin the original question and approved source, extract and verify assumptions, practice on new cases, compare against existing analytic and balanced controls, and add only independently checked new coverage. Prepare a new protocol and fresh evaluation cohort before training. Keep the existing source and definition gates; preserve the supplied-method portfolio as its reproducible baseline.
