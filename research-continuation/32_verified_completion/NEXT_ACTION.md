# Exact continuation

VC-001 is completed and its final cohorts are sealed. Continue from the actual live `runs/sera-completion-live` owner; preserve `runs/sera-grounded-live`, the owner audit store and both saved investigator candidates.

For another already-qualified conditional task, use a fresh job output and original task ID:

```powershell
.venv/Scripts/python.exe scripts/run_completion_bounded.py --seconds 60 --output runs/VC-live-next-001 --module experiments.verified_completion.runtime -- practice --id completion-practice-2 --seed 32411
```

This resumes the online policy and charges the existing numerical ledger. Read that ledger first; all published balances are historical. The process-tree cap remains 2 GiB with one numerical thread. No additional allowance is implied.

The next research integration should prospectively qualify human-source acquisition and interpretation through the retained original goal. The prepared Stage 31 OpenStax sources are preserved. Begin with development-only tasks that separately check source interpretation, physical assumptions, the learned proposal and independent consequences. Keep the closed novel-definition gate until new development evidence supports a replacement. Compare learned acquisition with the retained analytic/default and reading controls; separate factual evidence from conditional imagination. Use fresh held-out source groups and record the protocol before opening them.

The retained momentum task is an appropriate continuing goal, but its unadmitted meaning must first earn admission through targeted human material and independent practice. Do not substitute a manually supplied interpretation and call it learned. Do not reuse VC-001 or Stage 30 final cases as new training or selection data.
