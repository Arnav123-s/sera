# Quantitative interface mismatch, caught before training

The first targeted integration run failed four branch/resume checks: a sampled real acceleration was rationalized with denominator up to 10,000, while the retained FastMap reconstructs output fractions with denominator at most 120. Passing that new representation into the old certified route correctly failed its certificate.

The repair projects only the imagined execution view to a declared 1/60 acceleration grid. Both integrations then remain within the retained denominator contract. Every branch records its original mechanism, unrounded acceleration, exact execution value and bounded projection error (at most 1/120 acceleration units). The joint posterior and predictive reward remain unrounded. No old operator, source gate or final evaluation is changed.

The source and full failing log are preserved here; the complete resource charge remains in the live ledger. This is an engineering boundary repair, not new learned mathematical competence.
