# Admission decision

The predeclared VC-001 condition passed: the selected learned policy improves matched mean independent reward and prediction error against both the initial policy and random selection. It is admitted for **explicit conditional-practice use** through `CompletionSession` and the `practice` command.

Goal-information selection retains the default recommendation because its matched reward is higher. Both controls use the same public information. The `recommend` command applies that recorded decision without issuing off-policy reward credit.

The acquired object is the evidence-selection procedure for the supplied acceleration family. Its 321 policy parameters are registered on the continuing owner. The prior, uncertainty features, measurement mechanism, costs, source flags, checker and learning algorithm remain supplied. Repeated exposure to a finite world is counted as repeated exposure.

Conditional practice has its own source type. Acquisition changes a goal's joint completion weights; independently checked reward changes policy weights. Branch generation contributes zero factual observations. The earlier human source gates, original momentum task, source references and inherited abilities are retained. Any broader provider or mechanism requires its own prospective qualification.

The frozen matched, shifted and omitted cohorts remain sealed. Their outcomes do not select later parameters or become teaching data. [All results](final.json) · [Independent audit](integration-audit.json) · [Original protocol](PROTOCOL.md).
