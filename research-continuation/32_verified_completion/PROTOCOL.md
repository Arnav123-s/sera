# VC-001 — completion and checked credit on GroundR1

Frozen before training or opening new final cases. The Stage 30 owner is retained. The new controlled family is the v14 three-coefficient instantaneous acceleration response, `a = b*u - d*v + c`. An acceleration completion feeds the existing learned polynomial integration operator to imagine motion under an explicit constant-acceleration interval. One mechanism sample is used for every consequence within each branch.

This is model-conditional practice under a supplied response family. New numerical practice does not enter the human-language curriculum or the physical observation store. The 48-mechanism prior from v14 is reused with its original identity; no prior experiment is retrained. The user-requested human-book preparation stays preserved as Stage 31. The unknown momentum interpretation remains open behind the existing definition gate.

## Learned object and controls

A new registered per-candidate policy on the actual owner learns from independent log-score improvement minus acquisition cost. Its inputs are current query variance, candidate variance/covariance, declared measurement noise, price, duplicate/irrelevance indicators and STOP. These representations, known source flags, update algorithm and assessor are supplied. The selection weights are learned. A separate registered joint mean/covariance changes only after an eligible acquisition receipt.

Compare initial policy, trained policy, random acquisition including STOP, always STOP, parameter-information choice and goal-information choice. STOP has zero cost and zero update. Policy learning never reads hidden coefficients or the full reward table. Two seeds, 3211 and 3212; 800 minibatches of 64 independently generated practice cases per seed; Adam learning rate .003, entropy .01, gradient cap 2. Train seed 32001, 4096 worlds; development seed 32002, 512 worlds. Selection uses development mean independent reward. Half the cases have high acquisition costs to teach stopping. Fixed controls see the same public information.

Freeze selection before final seed 32991 (256 matched worlds), 32992 (256 shifted worlds) and 32993 (256 omitted-mechanism worlds). One chosen action is evaluated per case. Report paired reward, error, coverage, acquisition cost and STOP rate. These are 768 independent environments crossed with controls. No final case selects weights or thresholds. A learned controller is qualified for explicit conditional-practice use if it improves both initial and random controls on matched mean reward and error. The analytic control remains available and keeps default selection unless the learned method also improves its reward.

## Credit and persistence contract

Commit original goal, candidate features, sampled action, predictor identities and before/after predictions before the audit arrives. Reward is calculated internally from an independent receipt; caller-provided reward is forbidden. Consume each decision and audit once; refuse stale policy/predictor state, acquired/audit overlap, changed goals and relabeled imagination. Store the policy, optimizer, baseline, RNG, source identities and ledger. Hashes protect trusted-process integrity, not hostile-process isolation.

Use the actual retained integrator for branch execution and check with independent rational arithmetic. Preserve empirical observations, source gates, all old tensors and outputs. Check 26 taught definitions, 128 language probes, reading, exact math and empirical predictions; simulate interruption between decision, acquisition, audit and credit. Report source/semantic assumptions distinctly from exact arithmetic.

Every reservation and failure is charged to the existing live ledger. One CPU thread, 2 GiB process-tree committed memory, no new resource grant. Genuine defects are diagnosed and preserved; completed final evaluations stay sealed.
