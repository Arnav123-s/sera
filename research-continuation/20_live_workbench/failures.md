# Failures, repairs and withheld claims

- **A09 learned eta fails the strongest control.** It improves against the adaptive comparator but narrowly loses to persistence. Retained and not made the default; no sustained general self-improvement claim.
- **Full regression job hits its 80-second cap.** It emitted 202 successful test indicators; no final JUnit file was produced. Collection confirmed 233 cases. A separate 31-test remainder job passed. The full 80.0353 seconds remain charged, plus collection and remainder costs.
- **Browser HTML pattern error.** The browser's Unicode-set regular-expression mode rejected an unescaped hyphen in a name pattern. The pattern was corrected and evaluated in the browser; numerical validation had still rejected invalid task names independently.
- **Screenshot path restriction.** The browser tool could not write directly to the requested repository path. Returned image bytes were preserved locally through the filesystem tool; the images are screenshots, not generated substitutes.
- **Original process enumeration restricted.** Windows CIM process listing returned access denied. No unknown process was stopped. Owned budget/job state and the newly launched host PID were used instead.
- **Formal theorem-prover dependency absent.** Lean/lake were not found on PATH. No Lean execution, arbitrary proof learning or proof of an open problem is claimed.
- **Limited task family and retrieval.** Unknown requests need paired examples. Non-polynomial mappings can fail the withheld test; finite agreement can still miss an exception. The local material library is curated and source-labeled, not an autonomous web reader.

The original A08 uncertainty undercoverage, failed A06 shared-path retention and previous empirical guard failures remain valid predecessor evidence. Nothing in the workbench erases them.
