# Usable, continuing task execution

The requested outcome is a working learner that performs supported tasks and updates with arriving observations. A chat interface alone does not satisfy it. I am preserving the existing source architecture and making its boundaries explicit.

- [x] Reconcile clean SERA HEAD 636d52c with the v3 handoff, A08 checkpoint, preserved jobs and 278.8732422 remaining worker seconds.
- [x] Read the adopted continuation brief, START_HERE, WORKPLAN and source audit; reuse the matching recorded pack/release verification.
- [x] Implement persistent local operations on the real learner, including continuation and verified finite equations.
- [x] Add numerical data tasks, timestamp checks, causal feedback, forecasts, surprise flags and explicit predecessor versions.
- [x] Register each numerical task's compact acquired coefficients in the same R1 owner; preserve neural routes and reprove old guarded knowledge after owner changes.
- [ ] Train a distinct eta from measured learning outcomes; evaluate old/new K crossed with old/new eta on independent continuing lifetimes and actual successor generations.
- [ ] Diagnose or reject the eta candidate; never change a final protocol after observing its outcomes.
- [ ] Verify browser operation, inbox updates, exports, correction and resumption.
- [ ] Run meaningful regressions and integrity checks, audit the source architecture, record all costs, and deliver a running local application.

The server watches only its explicitly owned inbox and executes bounded local numerical workers. It does not observe unrelated personal files or silently expand the existing compute allowance. Idle HTTP/file polling is application service work, recorded separately from numerical worker wall time. Every numerical action uses the existing one-thread, 2 GiB process-tree supervisor and cumulative budget. No remote push, external model API, paid compute or arbitrary command execution is part of this release.

The numerical extension is a supplied AR(2)/drift/persistence method family with observed-error selection. It is a first practical data-processing application. Shared ownership is not evidence of representation transfer; real-time input access is not a claim of automatic broad knowledge or general intelligence. The attempted learned selector is separately trained and gated.
