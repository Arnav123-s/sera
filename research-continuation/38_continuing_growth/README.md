# Use the continuing learner

The admitted Stage 38 owner is saved at `runs/sera-growth-live`. Its twelve practice strands and all inherited knowledge remain together. [Results](report.md) · [Protocol](PROTOCOL.md) · [Audit](audit.json) · [Architecture](architecture-audit.md).

From the prepared repository workspace:

```powershell
.venv/Scripts/python.exe scripts/run_growth_bounded.py --seconds 60 --output runs/my-growth-request-001 --module experiments.continuing_growth.use -- request --text "wake me up at nine am on friday"
.venv/Scripts/python.exe scripts/run_growth_bounded.py --seconds 60 --output runs/my-growth-sum-001 --module experiments.continuing_growth.use -- sum --coefficients "2,1"
```

The request command returns the learned intent and retained entity tags. The summation command returns coefficients of the learned polynomial sum from zero through n−1, checked independently before being labeled certified algebra. Every output directory must be fresh; the wrapper reserves and charges the existing numerical allowance.

Use `scripts.continuing_use` for a batch through the latest admitted successor. It loads the owner once, supports attributed reading, four-language request interpretation, sum/integral calculation, taught-definition imagination and retained discovered routes. [Current usage](../../docs/START_HERE.md).

The complete source-attributed curriculum, snapshots, optimizers, RNG states, action/reward histories, comparison candidates and independent prediction witnesses are preserved in the research archive. `python -m scripts.growth_artifacts` checks their hashes and credit histories. No private assessment key is published. The original input sources retain the attribution and licenses recorded in [Stage 30](../30_grounded_books/ATTRIBUTION.md) and [the data guide](../../docs/DATA.md).

The admitted learner's knowledge improved on its prospectively held-back continuation cohort. Balanced remains its default practice schedule after the separately recorded future-procedure test. Subsequent experiments continue from this store while retaining it intact.
