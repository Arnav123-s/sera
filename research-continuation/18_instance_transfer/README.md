# Acquisition-dependent transfer within the original physical instance

Status: complete, independently audited and qualified within the declared scope. Corrected teaching reduced mean ordinary prediction MSE by 14.97% versus observed-only learning and 23.26% versus incorrect-K teaching. Every learning stream improved both comparisons; the largest measured old-score drop across 61 retained groups was zero.

The normalized circle-teaching experiment removes every fitted coefficient from its teaching distribution. I retain those numerical results and test a corrected task channel here. The future trajectories stay in the original physical coordinate system. I intervene on the actual preserved K before and after the original independent sensor correction; their initial neural parameters are exactly equal.

The optimizer, 514-parameter update scope, replay, parent-output anchors, batch sizes and checkpoint selection schedule stay fixed. Five new learning streams each compare 32 observed trajectories alone, those observations plus corrected conditional teaching, observations plus uncorrected conditional teaching, and an independent simulator-exposure control. No phase, center, radius or angular velocity enters the learned motion predictor. The assumed circle law and noiseless future simulator are supplied.

Before any training, the same conditional query schedule must produce different targets under the real K intervention. A positive final gate then requires the corrected teacher to beat both observed-only and uncorrected teaching by at least 10% mean MSE, improve both comparisons in every stream, have positive paired bootstrap lower bounds, and retain all 61 old groups within two points. This is an instance-specific readout-transfer test, not a learned shared representation, law discovery or improved investigator.

- [x] Verify mechanism tests and the actual K intervention.
- [x] Freeze source, runtime, counts, seeds and both planned comparisons.
- [x] Run all 20 bounded learning cells once.
- [x] Independently audit all 7,680 checkpoint predictions, geometric targets and retention.
- [x] Compare actual tensors in all 20 descendants: only the two readout tensors changed.
- [x] Integrate the preselected qualifying stream, restore facts and costs, reject stale bindings and re-prove the finite library.
- [x] Pass 210 regressions and exercise all three read-only resumption modes.
- [x] Preserve original sources and checkpoints, rejected candidates, costs and the architecture audit.

See the [complete report](report.md), [protocol](protocol.json), [independent audit](audit-summary.json), [parameter preservation](parameter-preservation-audit.json) and [usable saved learner](resume.md). The original operational store remains preserved; the new owner is an experimental research descendant. Faster-turning trajectories remain substantially harder (RMSE 0.423901), and shared representations, investigator eta and broad milestones remain unqualified.
