# CF-001: broader missing-input questions and composed discoveries

SD-001 is complete and preserved. It supplied a 66-record portfolio, with 60 exact routes and five of six language associations meeting its reserved threshold. Its one-missing-input search did not produce the position-only acceleration route needed by the preserved laboratory goal. No raw physical CSV was downloaded. A knowledge audit also identified additional native forward coverage: constant-force motion already available through the grounded parent, including a0=f/m. Those formulas must receive zero new-discovery credit even when expanded differently.

Continue the actual admitted SD owner. Preserve all earlier records, predictions, weights, costs and frozen evaluations. Reconcile novelty credit separately without rewriting SD's historical results. Generate new questions by hiding every pair of available observables, adding them to retained questions. Use already learned rule weights to compose two applicable routes as a third proposal mechanism beside sparse and dense fitting. This is an engineering extension to search; any chosen questions, fitted equations and composed routes are recorded experimental outcomes. No target formula is supplied.

Freeze three matched runs: learned investigators with seeds 4201 and 4202, and one deterministic balanced control with seed 4201, each for 384 decisions and three proposals per decision. Every proposal is durably committed before assessment. All numerical rows come from the same retained owner operators as SD-001. Conditions, unit types and the feature vocabulary remain explicit. Composed intermediate rules must come from the owned retained registry, and resulting coefficients receive the independent symbolic and execution checks.

New human language groups exclude every underlying request ID appearing anywhere in SD-001's fit/check/final partitions, including other translations. Hash modulo five assigns the remaining requests to fit (three buckets), check and final; use at most 768 per locale/partition. This is still a continuation within the parent's historical human training corpus. Proposal fitting uses owner predictions; assessment uses human labels. Suppress previously tried patterns for the same question so revisiting it generates another candidate. Both rejected and already rewarded patterns are excluded, and points require new independently correct coverage.

Correct the native-coverage reward before the new runs. Give existing forward and constant-force routes zero novelty points. Retain SERA's useful independent alternative input requirements, and give no repeated credit to a dominated expression. Keep the SD source/applicability gates. Twelve-strand inherited tensors and predictions must remain exact; only owned investigator and new rule parameters may change.

Select by independently supported new useful routes beyond the inherited portfolio, then checked reward. Evaluate all three predetermined candidates once on 96 fresh rational cases per newly retained equation and the new human final groups. Retain the development selection. Every new exact route must pass before integration. Reassess receipts, credit and one exact restart segment independently. Do not re-open SD's final or tune any candidate from CF final outcomes.

Resume the original physical observation goal only if the newly selected portfolio contains the required learned route. Preserve the old RETAINED_OPEN observation attempt. Use the unchanged measurement window, calibration/held-out split and numerical acceptance thresholds in SD-O01; freeze the actual successor equation before downloading observations. The source lesson remains outside learner proposal generation.

Resources use the user's standing approval: one CPU thread, 2 GiB committed process-tree cap, at most 900 seconds per job, explicit aggregate allocations and full measured costs, no paid services.
