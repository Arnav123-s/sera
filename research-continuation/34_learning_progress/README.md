# Verified learning progress

[Home](../../README.md) · [Results](report.md) · [Protocol](PROTOCOL.md) · [Audit](audit.json) · [Teaching and endpoints](selection.json)

I extended the continuing SERA owner with rewards for verified acquisition and a learned selector over practice order and update rate. Six curriculum strands reuse its existing human-book, source-reading and mechanics-method heads. The selected successor retains earlier knowledge and its independently qualified method portfolio.

The prepared local learner is `runs/sera-learning-live`. Commands below use the existing numerical ledger, one CPU thread and the 2 GiB process-tree cap; use a fresh output folder each time.

```powershell
.venv/Scripts/python.exe scripts/run_learning_bounded.py --seconds 45 --output runs/my-progress-status-001 --module experiments.learning_progress.bridge -- status
.venv/Scripts/python.exe scripts/run_learning_bounded.py --seconds 45 --output runs/my-progress-work-001 --module experiments.learning_progress.bridge -- solve --input research-continuation/33_capability_portfolio/example-work.json
.venv/Scripts/python.exe scripts/run_learning_bounded.py --seconds 45 --output runs/my-progress-reading-001 --module experiments.learning_progress.bridge -- read --input research-continuation/34_learning_progress/example-reading.json
```

The mechanics example returns **4 m/s** with its work-energy assumptions. The reader selects an original sentence from an attributed passage. The saved first reading demonstration is a preserved correction case: it chose a revenue sentence for a country question. [Prediction and human supporting sentence](reading-correction-open.json). For another reading task, supply a JSON object containing `question`, `source` and `sentences` (each sentence is an object with `text`). This operation returns attributed evidence; it does not promote a sentence into a measured observation.

Balanced practice remains the default. The learned scheduler and both seeds remain available in the research checkpoints. The selected successor completed 96 knowledge updates and 96 reward-predictor updates. Its four prior mechanics routes were freshly qualified together on 256 cases. The source and novel-definition gates retain their earlier contracts.

Public archives contain the initial/transition checkpoints, exact-resume boundary, prepared data, source identities and compressed prediction witnesses. `scripts/learning_artifacts.py` verifies them; `--restore` reconstructs missing research artifacts and refuses divergent local files. All intermediate working saves remain preserved locally and are indexed separately. Assessment signing keys stay in the original local authority; restoring public research artifacts does not mint a live qualification or create a numerical allowance.
