# SERA continuation audit — 2026-09-15

I am continuing V2-B: a persistent shared predictive learner with compact executable knowledge, competing explanations and evidence-directed investigation. The first geometric study is a controlled component experiment. It cannot complete the integrated architecture by itself.

Both archives are preserved and their 176 and 726 manifest entries match. The second archive embeds the first byte-for-byte. SERA is pinned at `a4047173fd33d27d33d8a151f32613eae343df56`; Kavi at `50f743cc44794b67bc1d30b927e25991197edce2`. Current local SERA additionally contains the optional R7 cloud experiment in `8dd87f9`.

Kavi's pinned upstream suite passes 321 tests. Its hierarchical registry has mutable ownership and interpreter-identity weaknesses exposed by bounded probes. The new bridge must validate and own its definitions; passing the upstream suite alone does not establish these contracts.

The first bundle's 57 checkpoint replays preserve all 411,648 correctness elements across 603 conditions. Two NLL differences slightly exceed its original `1e-6` tolerance. The strict original verifier therefore remains a failure; it is not being relabelled a complete pass.

The second bundle's 55 tests pass. Its original Windows replay stops at a source identity built from native path strings. Computing canonical POSIX paths from the unchanged source bytes recovers the recorded identity and passes all 1,050 evaluation artifacts, 240 teaching artifacts and 50 generation snapshots at the original `1e-12` tolerance. Fresh fitting, repair and replay complete in seven passing jobs. Exact archive/fresh parity has disclosed coefficient-hash differences and 101 numerical leaf differences in two records; discrete decisions agree. These are preserved alongside the successful fresh self-replay.

The packet's principal limitations remain consequential: supplied numerical coordinates and feature grammars; poor coverage when the representation is wrong; incorrect mechanisms accepted by small qualification banks; no reliable investigator-policy gain; incomplete learned applicability and shared transfer. The next experiments directly address model-class ambiguity and compact generators.

The probability-cloud branch is preserved with 1,260 models and 3,780 replayed query conditions. Its losses are quadratic in weights. It is an optional reference and does not justify a change to the main architecture or a quantum-advantage claim.

Detailed source and reproduction records live in `01_audit/`. Stage completion is recorded in `RESEARCH_STATE.json`; an implemented contract is kept separate from an experimentally established capability.

## New executed evidence

GG-P0-001 completes 640 paired cases and 5,120 learned/control artifacts over eight controls and four supplied-phase task families. All 20,480 query partitions replay exactly, and full same-seed refitting reproduces all model hashes. Independent calculations confirm the posterior moments, marginal likelihoods, class weights and NLL. Six requested task families and unordered geometry remain open.

Compact generators succeed when their supplied language matches the observations. On exceptions, residual memory improves local MSE but can worsen unseen-region coverage. This prevents a general applicability or uncertainty claim. The complete results and corrections are in `08_analysis/GG-P0-001/`.

Nineteen common event/graph tests establish parameter-free owner references, immutable definitions, transitive dependency invalidation, hypothetical isolation and explicit charged event replay. The registered generator workspace now passes 14 additional focused regressions and the full 156-test suite. Actual trained-parent descendants learn, correct and restore four generator fixtures while preserving 944 score elements across 40 capability groups exactly.

GG-ACT-001 completes 300 fixed-policy runs and 3,000 paid observations, with exact procedural replay and 12,000 independently checked Gaussian posteriors. Information gain avoids known uninformative noise, but space filling matches or beats it on the in-menu families. On omitted patterns, 99.738% mean best-class confidence coexists with only 51.3% nominal95 coverage. Learned applicability remains the immediate research gap.

Fresh pinned-SERA execution also passes its 71 tests and reduced shared-study plumbing. The actual full trained parent remains unchanged. The [final work-package comparison](01_audit/final-architecture-comparison.md) records implemented contracts, restricted demonstrations and remaining architecture gaps. The [results report](RESULTS_2026-09-15.md) links the full evidence.
