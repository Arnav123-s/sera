# Frozen finite contracts for SD-001

Two investigator seeds (4101, 4102), learned and balanced controllers, 256 decisions each. Every decision submits two independently recorded proposal procedures. The learned controller keeps 20% balanced exploration. Both visit the least-visited question identities before repeats. The inventory is generated from the actual retained operators, declared motion observables and the learner's own request predictions. SHA256 question identity modulo five reserves one fifth for future procedure evaluation.

Each equation investigation executes 48 imagined worlds using the parent's actual learned integral or sum matrices. Arithmetic expressions are built from observed variables, products, ratios and time powers, filtered by supplied physical units where relevant; at most 256 candidate features are available. Sparse pursuit (at most ten linear solves) and dense fitting independently propose rational coefficients with denominator at most 720. These are engineered proposal algorithms. Neither receives reference output values, target equations or checker residuals during proposal fitting. Undefined denominator features are omitted and every returned ratio carries explicit nonzero conditions.

The separate symbolic assessor expands the exact polynomial-model semantics and checks that the proposed rational identity has an identically zero numerator. Fresh final execution uses a separate rational implementation, including literal finite sums, on 96 new cases per retained route. This is conditional algebra, not evidence of physical occurrence. Native forward routes already available from the parent do not receive new-discovery credit. Canonical Laurent factors, identical applicability, dominated input requirements and redundant empirical coverage suppress repeated credit.

Human language comes from the existing MASSIVE-derived English, Spanish, French and German training records. Underlying request ID hashes separate fit/check/final groups across all translations (three/two remaining buckets); at most 768 records per locale and partition. The parent may already have seen these historical training records. Only text and the parent's predicted intents enter proposal fitting. Single tokens and contiguous pairs propose scoped associations with at least four fitting matches and at least 80% agreement with the parent's interpretation. Independent human annotations then require at least ten assessment matches and 90% accuracy for development retention. This finite threshold is not a universal applicability certificate. The final uses the same threshold on its separately reserved groups once; all failures remain visible and none changes the closed source gate.

Three supplied structured process examples train a three-action linear policy for 120 SGD updates. A separate 12-case context-strength/distractor check must choose all expected actions before research. This measures the taught process in the supplied status representation. The open-text descriptions accompany the traces; no claim of independently learning their representation is made.

Reward is 1 point for a supported new canonical relation that adds useful execution coverage, 0.4 for a new applicable route, up to 0.5 for newly correct human-annotation coverage, and up to 0.3 for a lower declared expression cost on a previously supported route. Incorrect or unsupported proposals cost 0.02. All attempted submissions count; repeated successes yield zero new credit. A reduced expression-operation count is recorded separately from measured wall-clock improvement. The question policy learns independently checked total reward with Adam at 0.005; its input contains prior coverage, visits and past progress, never current assessment labels. STOP and arbitrary source-quality learning are not silently introduced.

Select by development distinct supported routes, then verified reward. Open all four predetermined final candidates once for comparison, retaining the selected candidate regardless of their final ranking. Require every selected exact route to pass fresh execution before integrating. Human associations retain both development and final outcomes and confer no automatic request-override permission. Additions preserve all inherited tensors exactly. Independent audit checks commitments, stale/duplicate credit, exact restart, fresh executions and inherited owner identity/retention.

Actual external physical observations may be selected only after the relevant predictions are committed. They produce a separately labeled evidence receipt and cannot change the frozen models, scores or selected final. Do not describe generated conditional scenes as those observations.

After development selection, cross old/new retained relationship knowledge with old/new frozen investigator weights and balanced exploration on 32 reserved future-question decisions for each of two new seeds (41401, 41402). Keep starting visits and random state matched. Twelve trials receive the same proposal/check allowance. Record all new supported routes and total reward; require strictly more new retained routes than both controls for every seed/knowledge pairing to claim the procedure-improvement gate. These future outcomes cannot select a different primary final candidate. Rejected language patterns are excluded on later attempts at the same question, making counterexample-driven revision executable.

The implementation is frozen by SHA256 before `prepare`. Any genuine implementation defect is preserved with its source and results before a separately identified repair; it does not authorize final-driven retuning.
