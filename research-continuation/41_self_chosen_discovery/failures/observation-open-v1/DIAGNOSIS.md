# Preserved open observation goal

SD-O01 selected no eligible position-only acceleration route from the first admitted portfolio. Its `RETAINED_OPEN` result was preserved. Calling `observe` then correctly refused to substitute an external answer equation. No CSV was downloaded and no empirical evaluation was opened.

The resulting research question is whether broader combinations of missing inputs and composition of retained rules can acquire that route autonomously. CF-001 addresses that question across its complete inventory, alongside fresh human request groups. The resumed observation uses its independently selected owner, while retaining the original measurement window, calibration/final split and numerical thresholds. The original implementation, selection and failed invocation costs remain here.
