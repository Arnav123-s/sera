# SD-O01: prospective physical observation check

Candidate equations have already been committed in `runs/SD-study-001/proposals`, and their symbolic qualification is independent of this observation source. No external lesson, answer equation or measured acceleration entered candidate generation. The experiment operator has inspected a primary educational source to identify a real laboratory dataset and its documented measurement scope; the learner receives the CSV observations through the evaluation adapter.

Source: [Madison College Physics, Working with Data](https://madisoncollegephysics.net/comp/data.html), linked from its [Computational Physics collection](https://madisoncollegephysics.net/comp/). The provider identifies `data_freefall.csv` as measurements of a falling ball, supplies seconds/metres and documents rows 16 through 35 as its free-fall interval. This interval is curator supplied, not independently discovered by SERA. Velocity and acceleration columns will not be used as independent observations because they may be calculated from position.

Before downloading the raw CSV, freeze this check: select the development-selected learner's already certified constant-motion route that expresses acceleration from position, time, initial position and initial velocity, requiring no other observed field. The selection is deterministic: lowest declared expression cost, then canonical ID. If no such learned route exists, retain that missing capability and do not substitute a supplied answer equation.

Rearrange that learned relation generically to predict position. On the first ten rows of the provider's 20-row interval, fit only three nuisance parameters (initial position, initial velocity, acceleration) using the already fixed relation. Origin is one sample before the interval begins, avoiding zero denominators. Freeze these three fitted values and predict the final ten rows once. Retain every raw row, residual, fitted parameter, source hash and predictor/goal identity. No selected window, coefficient formula or acceptance threshold may change after this check.

Independent validation recalculates the predictions from the stored learned rational coefficients. The empirical confirmation gate requires position RMSE at most 0.02 metres and less than half that of a line fitted to the same first ten measurements. Also report a quadratic least-squares control and constant-position control at the same fitting budget. Matching the quadratic control is expected when it spans the same model family, and is not algorithmic superiority.

This check can grant one non-repeatable empirical-confirmation point tied to the original conjecture, observed-source hash, fitted predictor and held-out rows. It cannot change the primary SD-001 comparison, reopen its final, certify physical universality or make imagined scenes measured facts. Any empirical parameter update belongs to a separately versioned continuation of the actual selected owner, with the original owner intact.
