# IU-001: intervention-qualified understanding

Prospective final contract. Freeze this document, source files, packet verification and reconciled parent before opening seeds 47101–47108. Completed studies and development outcomes remain preserved.

The actual parent is the qualified Stage 46 learner, owner `376b3d076294ac5b92f9360bb27553edbd96d1aa2b12ae22913b55c0b135fa94`, on source `54e64945f3de4280387cfba5fa296225a568ccd3`. The Stage 44 pin in v17/v18 is historical. Preserve all previous checkpoints, 217 routes, 26 taught meanings, exact mathematics, languages, physical fields, source gates and corrected credit.

W01 will add explicit performed/observed, performed/unread, omitted-measurement and unavailable-sensor records around the existing R2 instrument. Its historical negative-outcome behavior remains unchanged. The independent fixture distinguishes an undisturbed plus state from a performed but unread Z measurement.

W02 will retain an original prediction-and-explanation goal together with passively equivalent mechanisms and permitted distinguishing interventions. Learned temporal coefficients and the associated uncertainty must live on the current R1 owner. A committed control, an independently obtained outcome and a check of what the control actually did precede any qualified revision. An answered conditional child does not certify the physical mechanism or close an unproved mathematical parent.

The proposed finite investigation concerns reversible static phase spreading and irreversible Markov loss with identical passive decay. Pulse implementation and additional pulse-induced loss are distinct candidate issues. Controls, mechanism vocabulary, typed variables, measurement meanings and assessor contracts are supplied engineering. Outcome counts train the retained numerical coefficients. Simulated observations remain explicitly labeled.

## Continuing owner and scope

Attach InterventionR1 to the same object held by the current language, mathematics, inquiry and physical-field routes. Register acquired temporal coefficients, counts and Fisher information as owner parameters/buffers. Preserve all 1,575 inherited tensor records, 217 routes, 26 meanings and 33 practical outputs. Additive code stays outside the historically fingerprinted core directory. No inherited core weights are retrained.

W01 uses schema sera.measurement-event.1. Occurrence (performed, omitted, unknown) is separate from outcome availability. Performed/unread applies the historical nonselective R2 channel; omitted preserves the input; unknown retains both alternatives. Control/propagation remains separate. Retain subject, action, elapsed time, provenance and evidence kind.

The supplied temporal model is P(+X)=(1+exp(-chi))/2 with chi=a*T+b*abs(A), or the installed richer candidate chi=a*T+b*abs(A)+c*N. T is elapsed seconds, A signed time under the applied pulse history, N applied pulse count. Nonnegative weights describe irreversible rate, static detuning width and pulse-induced loss. Passive data identify a noisy estimate of a+b, not a unique mechanism; retain its nonnegative a/b fiber and mark pulse loss unidentified.

The immutable original question asks for plus-X probability after 10 quarter-second ticks with a pulse at tick5, conditional on that pulse occurring, and for evidence distinguishing the explanations. Preserve its initial answer. A qualified empirical child does not prove a mathematical parent or physical uniqueness. Variables, vocabulary, model forms, optimizer and policies are supplied engineering. SERA learns numerical coefficients from outcomes.

## Frozen acquisition and comparisons

Six source families: static, Markov, combined rates, additional pulse loss, unreliable pulse implementation, and omitted quadratic pulse-count loss. Parameter ranges are explicit in intervention_source.py. Development:47001,47002. Final:47101–47108, giving48 distinct worlds and three policies per world,144 investigations. Policies are fixed local Fisher information, balanced allocation, and continued passive acquisition. They share underlying worlds but receive decision-bound, different outcome draws.

Each procedure receives8 passive controls plus12 acquired controls, each with1,024 shots. Independent selection:12 controls ×1,024 shots. Independent adequacy:20 controls ×2,048 shots. Total73,728 shot exposures per procedure,10,616,832 final exposures across policy arms. The supplied acquisition bank has15 programs. Selection and adequacy outcomes are excluded from coefficient fitting, and all access is charged.

Fit both candidates from counts at revisions8,11,14,17,20 using fixed PyTorch L-BFGS over softplus coefficients:100 maximum iterations, gradient tolerance1e-10, change tolerance1e-13, strong-Wolfe search. Retain fit costs and numerical state. Teacher parameters and final expectations are assessor-only.

The source generates independent individual Cauchy detuning paths, Poisson phase flips, pulse-loss flips and binary readouts; it never calls the learner's forward function to create labels. A separate control log groups outcomes by the actual applied history, including dropped pulses. Records bind command, decision, source, subject, purpose, actual controls and counts. Digest checks provide integrity within the trusted assessor boundary, not authentication of arbitrary external senders. All data is explicitly SIMULATED_OBSERVATION, not laboratory measurement or human-authored language data.

Every information-policy world has these matched controls: frozen initial passive fit; independent two/three-coefficient SciPy likelihood fits and validation-selected classical model; command-trusting model ignoring the actual monitor; actual current R1 encoder/recurrent features with a new ridge readout selected from0.001,0.01,0.1; and existing four-dimensional rank-two complex R2 ControlledInstrument fitted for180 Adam steps, selection every20 steps. R2 learns channels and final measurement with supplied preparation and no invented intermediate measurement. R1 gets an explicit numeric encoding. Controls have identical acquisition/selection access, no final labels. Preserve weights, validation traces and costs. Independent exact physical expectations are an assessor reference. Retained exact mathematics is separately checked for preservation.

## Qualification and final decision

Select the pulse-loss candidate only if selection negative log likelihood improves by more than0.0005 per shot. Require full acquired-design rank within the selected model. An independent bounded SciPy likelihood fit must agree on selection/adequacy predictions to at most0.002.

Adequacy uses fresh count residuals, with binomial variance plus local Fisher prediction variance: diagnostic chi-square tail probability at least0.001, maximum absolute standardized error at most5. This approximate finite gate is distinct from model confidence and completeness. Report confidence as a local approximation conditional on the supplied model; unidentifiable query directions receive no local standard error. Preserve false admissions and all raw diagnostics.

Qualification binds source, original goal, evidence revision and model states. New evidence or altered weights invalidates it. Keep previous receipts, competing explanations and failed attempts. Reject reused assessment after learning. No new reward ledger, points or permissions are added.

Primary final queries are unique histories disjoint from every initial, candidate, selection and adequacy control. For durations5,7,9,11,13,15 ticks and pulse counts0–4, choose up to three unseen histories per pair by deterministic hash, independent of outcome labels. Report the actual bank size and exposure count. Separately report15 longer-duration/more-pulse extrapolations. Assess the unchanged original goal only after qualification. Do not train or select using final outcomes.

Integration requires at least80% of the40 supported information-policy worlds to qualify; all48 passive-only investigations to stay unqualified for the intervention question; at most25% of8 omitted-family information-policy worlds to qualify; every qualified supported world's final probability MSE below0.0025; and average original-goal squared error across supported worlds below half its initial value. Report all policies, controls, false admissions and failures. Passing this gate does not establish that information selection beats balanced practice. Freeze before final; do not retune on completed final data.

## Replay, retention and resumption

Independently replay saved probabilities, inverse fits, source outcomes, decisions and actual-control receipts. Replay R2 complex weights stored as real/imaginary arrays. Verify all inherited tensors,33 prior tasks,26 meanings,217 routes, source gate and corrected credit. Compare parent state with only the additive owner identity adjusted. Preserve exact earlier mathematical and language routes.

Checkpoint before an intervention outcome, after each observation and after each completed fit. Resume exactly at these transaction boundaries, including a saved observation awaiting fitting. A fit interrupted within its optimizer may restart that bounded unfinished fit; it must not repeat completed observations or investigations. Test actual-owner interruption and identical continuation. Completed inquiries remain read-only.

Portable replay allows numeric diagnostics atol1e-10/rtol1e-8; source, evidence, decisions, stored receipts and admission decisions match exactly. The independent inverse-fit gate retains0.002. Float32 R2 replay allows2e-6 probability error. Preserve the candidate successor even if promotion is rejected.

Standing authorization removes local time limits. Retain one CPU thread,2GiB process-tree cap, no paid service, one owned numerical job, exact cost accounting and all predecessor state. Windows stays awake only during the owned job. No other session is stopped.
