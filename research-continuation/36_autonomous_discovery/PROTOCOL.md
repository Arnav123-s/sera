# AD-001: self-selected investigations of retained computation

Continue the exact Stage 35 owner. No new training sources, completed equations, task-specific relation hints or manual choices after this protocol is frozen enter the run. The search machinery, proof contracts and finite budget are supplied engineering. The learner inventories its retained integral and summation maps and the previously available time-weighting operation; it identifies primitive families absent from its relation library.

The finite expression space contains identity and all compositions of the available I/M/S primitives through length three. Input degree is at most three, preserving the taught degree-five input boundary at the last learned primitive. The learner chooses among unvisited depths using its inherited reward logits with an explicit uncovered-family exploration term. It generates eight integer-coefficient imagined polynomials for each selected investigation. It uses two supplied general proposal procedures: numerical dependency fitting and exact elimination of the outputs of its learned operators. Checker outputs never supply proposal coefficients. Shared-prefix caching is supplied engineering and is counted explicitly.

The separate checker certifies candidates with rational arithmetic on the polynomial basis and linearity. Existing relations, scalar aliases and certified left/right compositions of retained relations receive zero new-discovery credit. One new independent relation modulo that retained closure earns one point. Agreement between two proposal methods is recorded but not rewarded as two discoveries. Coefficients become registered executable owner parameters. The depth policy receives independently checked discovery reward less .01 per generated program. All proposed, rejected and redundant candidates and both procedures' work are retained.

Stop when the three-depth frontier is exhausted or the owned worker reaches its resource bound. Save after each investigation. Replay from the first checkpoint using the exact RNG, remaining frontier, policy and accepted rules. Preserve all inherited tensor values. After discovery is sealed, evaluate each admitted relation on 32 fresh integer-coefficient polynomials from seed 36991. Independently check a directly computed target and the alternative route, including actual use of the newly retained coefficients. No discovery/procedure updates occur in this evaluation. Original goals and source gates remain preserved.

The scope is discoveries new to this learner's library, with finite mathematical assumptions. A new relation modulo the current composed closure is separate from mathematical priority or unrestricted physical discovery. Procedure improvements require a separate matched acquisition test; this run does not grant points for an engineering optimization or for two algorithms finding the same result.

## Pre-evaluation execution repair

The first run stopped before saving an investigation or opening final data: an imagined composition failed the existing learned primitive checker. Its original sources, freeze and costs remain under `failed-first/` and `runs/AD-run-001`. The corrected run uses `runs/AD-study-002` and refreezes before continuing. When the inherited denominator-120 reconstruction fails, a general candidate procedure proposes bounded rational reconstructions of the same learned weight output, doubling the denominator allowance through 30,720. The independent checker provides acceptance only. SERA retains the first verified setting that improves on its previous setting, earning one procedure-progress point per verified advancement. Candidate values come from learned outputs, never from checker-produced answers. No old operator weights change. Final evaluation freezes these settings and reports old-versus-retained decoding on identical intermediate inputs, counting withheld cases separately. This is a narrow numerical procedure acquisition test; broader optimizer improvement remains a separate contract. Unresolved execution gaps remain explicit and receive no discovery credit.
